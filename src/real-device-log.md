# 実機で確かめた記録 — `src/` を本物のスプレッドシートに載せる

**実装者の道具である。** この文書は**担当者の操作**（→ [`docs/tech-requirements.md`](../docs/tech-requirements.md) 2「担当者がやることの全部」の 10 行）**も、画面の枚数も 1 つも増やさない。**

**載せ方そのものは [`README.md`](README.md) の「テンプレートの作り方」が持つ。ここに二重に書かない。**

| ここが持つもの | |
| --- | --- |
| **手元で分かる範囲と、実機でしか分からない範囲の線** | 同じことを二重に数えないため |
| **実機で見る 19 項目**と、**確かめた／確かめていないの別** | |
| **数える側を入れてから見た 4 つ** | [#185](https://github.com/jun-eg/school-festival-shift/issues/185)。**未了の数 ／ 貼り忘れ ／ 検証結果への書き込み ／ 条件入力が型に乗るか** |
| **M1 ② の経路を実機で 1 周通した記録** | [#199](https://github.com/jun-eg/school-festival-shift/issues/199)。**段 1〜10 の「通った／止まった」** |
| **走らせた 1 回ごとの記録** | 通った／通らなかった・外れた前提 |
| **初回承認で出た画面の一覧** | 撮った画像がどれで、どの順に出たか。**スコープを書く前・1 スコープ・3 スコープの 3 組ある** |
| **確認に使うファイルの規約** | 実データを 1 行も入れない・画像の置き場 |

**記録の中の名前は、そのとき見たものである。** **`.gs` の名前や関数名が日本語で書いてある行は、
そのとき実機に貼ってあった名前である** — **いまの `src/` は、ファイル名も関数名・変数名も英字である**
（→ [#175](https://github.com/jun-eg/school-festival-shift/issues/175) ／
[#178](https://github.com/jun-eg/school-festival-shift/issues/178)）。**見たものを後から書き換えない。**
**リンクは、同じファイルを指したまま新しい名前に直してある。**

**判定はここが持たない。** 下の表の 5 は [#136](https://github.com/jun-eg/school-festival-shift/issues/136) が、
**要求する権限スコープ**は [`README.md`](README.md) の「要求するのは 3 スコープである」が
（→ [#139](https://github.com/jun-eg/school-festival-shift/issues/139)）、
担当者が自力で通せるかは [#160](https://github.com/jun-eg/school-festival-shift/issues/160) が持つ。
**ここが持つのは、判定を載せる土台だけである。**

## 手元で分かる範囲と、実機でしか分からない範囲

**手元の検査 14 本で分かるのは、入力の型と、取り込みの畳み方と、希望の展開の仕方と、違反と未充足の数え方と、フォームの定義と作り方の手順と、組み立ての手順と、コアと殻の境目と、走る前の構造の検証だけである**（→ [`README.md`](README.md) の「確かめ方」）。

| 何を見ているか | どこで | 件数 | 実機でどう扱うか |
| --- | --- | --- | --- |
| **定義そのもの**（5 枚・担当者が書く側・保護する側・区画・列名） | [`sheet-layout.test.mjs`](sheet-layout.test.mjs) | **22** | **実機で数え直さない。**[`sheet-layout.js`](sheet-layout.js) は **`SpreadsheetApp` を 1 度も掴まない**（→ 6 の #8）ので、**実機と手元で結果が変わる余地が無い** |
| **入力の型**（6 種類だけであること・枠の刻み・揃っていない値で止まること・[`data/`](../data/) の**希望データのモック 1 本**が型 #6 に乗ること。**確定シフト 4 本が型に乗るか（→ M1 ①）は数えない** — [`scripts/M1①の判定.mjs`](../scripts/M1①の判定.mjs) が持つ） | [`input-types.test.mjs`](input-types.test.mjs)。**スプレッドシートを 1 つも作らない** | **25** | **実機で数え直さない。**[`input-types.js`](input-types.js) も **`SpreadsheetApp` を 1 度も掴まない**。**食わせているのは [`data/`](../data/) のモックで、実機のセルではない** |
| **コアそのもの**（配列を渡して配列が返る・未了の名指し・揺れたら止まる） | [`core.test.mjs`](core.test.mjs)。**スプレッドシートを 1 つも作らない** | **30** | **実機で数え直さない。**理由は上と同じである（**掴んでいないものは実機でも変わらない**）。**掴んでいないこと自体を、この検査が毎回数えている** |
| **取り込みの畳み方**（規則 2 の ①〜④・[`data/`](../data/) の**希望データのモック 1 本**が 50 行 → 39 件になること・1 行に決まらない行で止まること） | [`take-in.test.mjs`](take-in.test.mjs)。**スプレッドシートを 1 つも作らない** | **28** | **実機で数え直さない。**[`take-in.js`](take-in.js) も **`SpreadsheetApp` を 1 度も掴まない**。**食わせているのは [`data/`](../data/) のモックと手で組んだ回答の行で、実機のセルではない。****実機で残るのは「タイムスタンプのセルがどう返ってくるか」だけ**で、それは殻の側である（→ 下の 8） |
| **希望の展開の仕方**（規則 1 の ①〜④・3 の境界値と特別扱いの 4 つ・[`data/`](../data/) の**希望データのモック 1 本**の 39 人が**終端 ≤ 始端の 3 人で止まり、残る 36 人 × 4 日で 144 件・のべ 1191 枠**になること） | [`expand.test.mjs`](expand.test.mjs)。**スプレッドシートを 1 つも作らない** | **37** | **実機で数え直さない。**[`expand.js`](expand.js) も **`SpreadsheetApp` を 1 度も掴まない**。**食わせているのは [`data/`](../data/) のモックと手で組んだ型で、実機のセルではない。****営業時刻の置き方は前回の値である**（→ 7 の「前回の 5 時刻の置き方」）ので、**実機の条件入力とは別物である** |
| **生成の組み方**（違反 0 の案が 1 つ出る・足りない枠が全部名指しで出る・同じ入力から同じ案・外部のソルバーを読んでいない・5-5 の適用の順序どおりに置く。[`data/`](../data/) の**希望データのモック 1 本**の **36 人 × 88 枠で違反 0・需要のべ 704 が 置いた 589 ＋ あと何人 115** になること） | [`generate.test.mjs`](generate.test.mjs)。**スプレッドシートを 1 つも作らない** | **38** | **実機で数え直さない。**[`generate.js`](generate.js) も **`SpreadsheetApp` を 1 度も掴まない**。**食わせているのは [`data/`](../data/) のモックと手で組んだ型で、実機のセルではない。****実機に残るのは所要時間だけ**である。**手元のコアは測った**（[#152](https://github.com/jun-eg/school-festival-shift/issues/152)。**上限の中で終わった。秒も余裕の倍率も [`../docs/tech-requirements.md`](../docs/tech-requirements.md) の更新ログが持つ。ここに写さない**）。**`SpreadsheetApp` の往復と実機の V8 の速さも、実機で 1 回走らせて見た**（**2026-09-22。上限の中で終わった** → 下の「実機で見る項目」の 14） |
| **違反の数え方**（数えるのは 5 つで ⑥ を数えない・1 件ずつ仕込むとそれぞれが名指しで出る・判定できない行で止まる） | [`count-violations.test.mjs`](count-violations.test.mjs)。**スプレッドシートを 1 つも作らない** | **32** | **実機で数え直さない。**[`count-violations.js`](count-violations.js) も **`SpreadsheetApp` を 1 度も掴まない**。**食わせているのは手で組んだ割り当ての行で、実機のセルではない** |
| **割り当ての敷き方**（従来のシフト表の形に敷ける・行の並びが学籍番号の昇順・敷いて戻すと元に戻る・当てるのが見出しの時刻・載らないもので止まる） | [`assignment-grid.test.mjs`](assignment-grid.test.mjs)。**スプレッドシートを 1 つも作らない** | **18** | **実機で数え直さない。**[`assignment-grid.js`](assignment-grid.js) も **`SpreadsheetApp` を 1 度も掴まない**。**実機に残るのは「見出しの時刻がどう返ってくるか」だけ**で、それは殻の側である（→ 下の 8。**確かめた（2026-09-23）。`Date` で返り、`normalizeValue` が揃えている**） |
| **未充足の数え方**（数えるもとは 2 つ・切り方が希望と逆向き・必要人数と指定枠の不足がどちらも名指しで出る・数えられない需要で止まる） | [`name-unmet.test.mjs`](name-unmet.test.mjs)。**スプレッドシートを 1 つも作らない** | **28** | **実機で数え直さない。**[`name-unmet.js`](name-unmet.js) も **`SpreadsheetApp` を 1 度も掴まない**。**食わせているのは手で組んだ条件と割り当ての行で、実機のセルではない** |
| **殻の読み書き**（表現を揃える・区画ごとに切る・段が欠けているあいだは書かない・崩れていれば走らない） | [`shell.test.mjs`](shell.test.mjs)。**偽のスプレッドシートの上である** | **27** | **セルがどう返ってくるかだけが実機で決まる。****確かめた（2026-09-21）。偽のスプレッドシートと同じだった**（→ 下の 8）。**残りは「手元と同じ結果になるか」で、殻を呼ぶ側（生成 → [#151](https://github.com/jun-eg/school-festival-shift/issues/151)）は入った。実機で 1 回走らせたときに見る**（→ 下の 14） |
| **フォームの定義そのもの**（4-1〜4-3 の表との突き合わせ・入る側の 6 項目・前回の回答 50 行を弾かないこと） | [`form-definition.test.mjs`](form-definition.test.mjs)。**フォームを 1 つも作らない** | **26** | **実機で数え直さない。**[`form-definition.js`](form-definition.js) は **`FormApp` も `SpreadsheetApp` も 1 度も掴まない**。**ただし「定義どおりのフォームが本物の上で作れるか」は別である**（→ 下の 10〜13） |
| **走る前の構造の検証**（シートの有無・見出し・列数を名指しする） | [`verify-structure.test.mjs`](verify-structure.test.mjs)。**読むだけの偽のスプレッドシートの上である** | **18** | **「手元と同じ結果になるか」だけを見る。****まだ見ていない** — 崩してから走らせる操作は、**殻を呼ぶ側（生成 → [#151](https://github.com/jun-eg/school-festival-shift/issues/151)）が入ったので、実機で 1 回走らせたときに見る**（→ 下の 14）。実機で違いが出るなら、それは**`getLastColumn` と `getValues` の返り方**である（→ 下の 8 と同じ所） |
| **組み立ての手順**（作る・保護する・2 回目・止まる） | [`build-template.test.mjs`](build-template.test.mjs)。**偽のスプレッドシートの上である** | **9** | **「手元と同じ結果になるか」だけを見る。**中身を数え直さない（→ 下の 1〜4） |
| **フォームの作り方**（定義の順に置く・画像を差す・回答先を向ける・回答シートに繋ぐ・止まる） | [`build-form.test.mjs`](build-form.test.mjs)。**偽のフォームと偽のスプレッドシートの上である** | **34** | **「手元と同じ結果になるか」だけを見る。****まだ見ていない。****偽のフォームが真似ているのは手順だけ**なので、**本物でしか分からないものが 4 つ残る**（→ 下の 10〜13） |
| **実機でしか分からないもの** | どの検査にも無い | — | **新しく見る**（→ 下の 5〜9・10〜13） |

**二重に数えないとはこのことである** — 実機で増えるのは **5〜9 の 5 つ**と**フォームを作る側の 10〜13 の 4 つ**と
**生成を 1 回走らせる 14** と **M2 を判定する 15** と、**手直し・印・画像の 16〜18** と **指標まで書く 19** で、**1〜4 は「偽を本物に置き換えても同じか」の確認である。**

## 実機で見る項目 — 4 ＋ 5 ＋ 4 ＋ 1 ＋ 1 ＋ 1 ＋ 1 ＋ 1 ＋ 1 ＋ 1

### 1〜4: 手元の 9 件に対応する（同じ結果になるかだけを見る）

| # | 実機で見ること | 手元のどれに対応するか | 確かめたか |
| --- | --- | --- | --- |
| 1 | **5 枚が構成の並びででき、既定の「シート1」が消える** | ①（5 枚・見出しの行・回答の 10 列） | **確かめた（2026-09-20）。手元と同じ。****構成が 8 枚になった後も確かめた（2026-09-23）** — **割り当ての 4 枚ができ、50 列に広がった**（→ 下の「[割り当てを 4 枚のマス目にしてから走らせた](#割り当てを-4-枚のマス目にしてから走らせた213)」） |
| 2 | **生成シート 3 枚に「警告のみ」の保護がかかる** | ②（保護は 3 枚・警告のみ・説明つき） | **確かめた（2026-09-20）。手元と同じ** |
| 3 | **2 回目を走らせても形が変わらず、何も作らない** | ③ | **確かめた（2026-09-20）。手元と同じ** |
| 4 | **見出しを書き換えてから走らせると名指しで止まり、書き換えたセルを上書きしない** | ④ | **確かめた（2026-09-20）。手元と同じ** |

### 5〜9: 実機でしか分からない

| # | 何が分からないか | 外れたらどうするか | 判定を持つのはどこか | 確かめたか |
| --- | --- | --- | --- | --- |
| 5 | **コピーでバインドしたスクリプトが渡るか**（→ 6-1 の #3） | **配り方を変える**（担当者の操作は「コピー 1 回」のまま）。**変えられないなら 6 の #1 が崩れるので ADR を 1 本起こす** | **[#136](https://github.com/jun-eg/school-festival-shift/issues/136)。**ここは土台だけ | **確かめた（2026-09-21）。渡った。**下の「コピーで何が渡ったか」 |
| 6 | **「警告のみ」の保護が、担当者の目にどう出るか** | **保護の強さの判断は動かない** — **持ち主は締め出せない**（→ [`README.md`](README.md)「保護は『警告のみ』である」） | **ここ** | **確かめた（2026-09-20）。**下の「保護がどう出たか」 |
| 7 | **日本語の関数名が、メニューの項目から呼べるか** | **メニューから呼ぶ関数だけを英字の名前にする**（[`menu.js`](menu.js) の `関数` の値。**表示名は変えない**） | **ここ** | **確かめた（2026-09-20）。呼べた。改名は要らない**（**その後 [#178](https://github.com/jun-eg/school-festival-shift/issues/178) で、呼べるかどうかとは別の理由から英字に決め直した。表示名は動いていない**） |
| 8 | **`getValues()` が返すセルの表現**（日付・時刻・日時が `Date` で来るか文字列で来るか。**ロケールと書式で揺れる** → 6 の #8 の理由 ③） | **[`shell.js`](shell.js) の `normalizeValue`（当時の `表現を揃える`）の見分け方を直す。****判断は動かない** — 揃えるのが殻の仕事であること自体は 6 の #8 である。**コアには手が入らない** | **ここ** | **確かめた（2026-09-21）。手元の前提どおりだった。**下の「[セルの表現がどう返ってきたか](#セルの表現がどう返ってきたか項目-8)」 |
| 9 | **宣言した `oauthScopes` で初回承認が通り、どの画面が何枚出るか**（→ 6-1 の #4。**要求するスコープで決まる** → [`README.md`](README.md) の「要求するのは 3 スコープである」） | **足りないスコープがあれば走らせたときに権限のエラーで落ちる** — **落ちた先を見て [`appsscript.json`](appsscript.json) に足す。****広いほうへ戻すのは最後である**（戻すなら理由をここに書く）。**判断（掴むのは `getActive()` だけ）は動かない** | **[`README.md`](README.md)**（スコープの一覧）と、**画像は [`docs/画面の記録/`](../docs/画面の記録/)** | **確かめた（2026-09-21）。1 スコープで足りた。画面は 2 枚だった**（前は 4 枚）。下の「[スコープを書いた後に出た画面](#oauthscopes-を書いた後項目-9)」。**その後 [#144](https://github.com/jun-eg/school-festival-shift/issues/144) で 3 スコープになった** — **いまの一覧で何枚出るかは下の 13 が見る** |

**ここで見ないもの。** **担当者が自力で 1 周通せるか**は [#160](https://github.com/jun-eg/school-festival-shift/issues/160) が見る（M5。**実地でしか分からない**）。

### 10〜13: フォームを作る側（[#144](https://github.com/jun-eg/school-festival-shift/issues/144)）— **4 つとも見た**

**偽のフォームの上で見たのは手順だけである**（→ [`build-form.test.mjs`](build-form.test.mjs)）。
**本物の Google フォームでしか分からないものが 4 つある。**

| # | 何が分からないか | 外れたらどうするか | 判定を持つのはどこか | 確かめたか |
| --- | --- | --- | --- | --- |
| 10 | **長文回答の正規表現と、そのエラーメッセージの文言がスクリプトから設定できるか**（**4-3 の句点の揺れ ◎ を含む**。→ 6-1 の #1） | **フォームを「完成品の複製」に切り替える**（6 の #6 の却下側）。**設問と正規表現だけ揃えて文言を落とす、はしない** — 仕様 #2 の判定が**差分 0** だからである。**切り替えると M1 ② の数え方を見直す** | **[#145](https://github.com/jun-eg/school-festival-shift/issues/145)。**ここは土台だけ | **確かめた（2026-09-22）。設定できた。**下の「[正規表現とエラーメッセージがどう入ったか](#正規表現とエラーメッセージがどう入ったか項目-10)」 |
| 11 | **`setDestination` が [`appsscript.json`](appsscript.json) の 3 スコープで通り、フォームが作る回答シートが 1 枚で、その見出しが構成の 10 列と一致するか**（→ [`README.md`](README.md) の「フォームは定義から作る」） | **足りないスコープがあれば走らせたときに権限のエラーで落ちる** — **落ちた先を見て足す**（広いほうへ戻すのは最後である）。**見出しが違えば繋がずに止まる**ので、**見てから [`sheet-layout.js`](sheet-layout.js) と定義のどちらを直すか決める**（黙って直さない） | **ここ** | **確かめた（2026-09-22）。3 スコープで通り、回答シートは 1 枚で、見出しは 10 列とも一致した。**下の同じ節 |
| 12 | **担当者が選んだ画像が、ダイアログ越しに画像アイテムへ入るか**（`showModalDialog` → `Utilities.newBlob` → `setImage`） | **受け渡しが通らないなら、画像の差し方を変える**（**担当者に要求するのは「選ぶこと」だけ** → 2 の一覧 3。**ここは動かさない**） | **ここ** | **確かめた（2026-09-22。残り半分は [#199](https://github.com/jun-eg/school-festival-shift/issues/199) で見た）。****ダイアログの `<input type="file">` に載せた画像が、そのまま画像アイテムに入り、入る側の画面に出た。****OS のファイル選択ダイアログ（Chrome の UI）だけは踏んでいない** — 下の「[ダイアログの入口から 1 周通した](#ダイアログの入口から-1-周通した199)」 |
| 13 | **3 スコープになった初回承認で、どの画面が何枚出るか**（→ 6-1 の #4。**1 スコープのときは 2 枚だった**） | **画面が増えるのは、要求が増えたぶんである。****撮り直して連番を足し、[#159](https://github.com/jun-eg/school-festival-shift/issues/159) の手順書に貼る画像も差し替える**（→ [`README.md`](README.md) の「手で書いた以上、自動では増えない」） | **スコープの一覧は [`README.md`](README.md)、画像は [`docs/画面の記録/`](../docs/画面の記録/)** | **確かめた（2026-09-22 → [#199](https://github.com/jun-eg/school-festival-shift/issues/199)）。****4 枚だった**（1 スコープのときは 2 枚）。**「このアプリは Google で確認されていません」が戻ってきて、同意画面はスコープを 1 つずつ選ぶ形になっていた。**下の「[`oauthScopes` が 3 つになった後（項目 13）](#oauthscopes-が-3-つになった後項目-13)」 |

### 14: 生成を実機で 1 回走らせる（[#152](https://github.com/jun-eg/school-festival-shift/issues/152) の残り）— **見た（2026-09-22）**

**手元で測れるのはコアだけである**（[`../scripts/生成の所要時間.mjs`](../scripts/生成の所要時間.mjs)。
**コアは `SpreadsheetApp` を 1 度も掴まない** → 6 の #8）。**手元の秒は
[`../docs/tech-requirements.md`](../docs/tech-requirements.md) の更新ログが持つ。ここに写さない。**
**実機の秒はここが持つ**（下の節）。

| # | 何が分からないか | 外れたらどうするか | 判定を持つのはどこか | 確かめたか |
| --- | --- | --- | --- | --- |
| 14 | **メニューの「生成」を 1 回押したときの、実機の 1 実行の所要時間**（**`SpreadsheetApp` の往復と、実機の V8 の速さが乗る**。→ 6-1 の #2） | **上限（6 分 ◎ — 個人の Gmail の側）に当たったら、生成をブラウザ側に移す**（→ 6 の #4 の却下側）。**方式（6 の #3）は変えない。****移したら、同じ入力から同じ案が出ることを確かめる**（→ 6 の #3 の理由 ③） | **ここ。**手元のぶんは [`../scripts/生成の所要時間の宣言.json`](../scripts/生成の所要時間の宣言.json) が線を持つ | **確かめた（2026-09-22）。上限の中で終わった** — **書き戻しまで含めて 4.354 秒 ／ 5.374 秒**（2 回。**上限 360 秒に対して 83 倍 ／ 67 倍**）。下の「[生成を実機で走らせたときの秒](#生成を実機で走らせたときの秒項目-14)」 |

**この回に一緒に通ったもの**: **構造の検証が止めずに通り**（[`verify-structure.js`](verify-structure.js)）、
**殻が読んだ値が型に乗った**（[`shell.js`](shell.js)。**揃えたあとの表現で止まっていない**）。
**まだ見ていないのは「崩したシートで名指しして止まるか」のほうである** — **崩す操作をしていない**
（→ 上の表の [`verify-structure.test.mjs`](verify-structure.test.mjs) の行）。

## 生成を実機で走らせたときの秒（項目 14）

**2026-09-22。上限の中で終わった。**[#199](https://github.com/jun-eg/school-festival-shift/issues/199) で使ったスプレッドシート
（**M1 ② の確認に使ったもの。回答シートにモックの行が入っている**）**の上で、`src/` の実装をそのまま走らせた。**

**走らせたのは 5 回である。****上限は 360 秒**（Apps Script の 1 実行あたり 6 分。→ 6-1 の #2）。

| 何を測ったか | 秒 | 上限までの余裕 |
| --- | --- | --- |
| **書き戻しまで含めた 1 実行**（**本番に一番近い** — 下の「仮の段」を参照） | **4.354 ／ 5.374** | **83 倍 ／ 67 倍** |
| **いまの実装のままの 1 実行**（**指標の段が未了なので、殻は 1 枚も書かない** → [`shell.js`](shell.js) の `writeOutputs`） | **3.478 ／ 3.678 ／ 4.095** | **88〜104 倍** |
| **内訳: 読み**（`SpreadsheetApp` の往復。**2 周目の値である**） | **1.040 ／ 1.067 ／ 1.561** | — |
| **内訳: コア**（`build` 1 周。**2 周目の値である**） | **0.245 ／ 0.272 ／ 0.457** | — |

**入力は手元の実測と同じである**（→ [`../scripts/生成の所要時間の宣言.json`](../scripts/生成の所要時間の宣言.json)）—
**回答 46 行**（**モック 50 行のうち、終端 ≤ 始端を書いた 3 人を外したもの**）**・枠 88。**
**出たのは割り当て 660 行と検証結果 83 行**で、**書き戻しまで含めた回では、その行が実際に割り当てシートに入った。**

**分かったことは 2 つある。**

**① 実機のコアは手元より速い。****手元の Node が 0.73 秒**（→ 更新ログ）**なのに対し、実機は 0.245〜0.457 秒である。**
**「実機が何倍遅ければ上限に当たるか」を手元では測れない**（→ 宣言の「測らないもの」）**と書いたが、
実際には遅くなかった。**

**② 1 実行の時間を決めているのは `SpreadsheetApp` の往復のほうである。**
**コアは 1 実行 4 秒のうち 0.3 秒ほどしか占めていない。**
**生成をブラウザ側に移しても（→ 6 の #4 の却下側）、移るのはこの 0.3 秒の側だけである。**

**仮の段について。** **指標の段（[#154](https://github.com/jun-eg/school-festival-shift/issues/154)）がまだ無いので、
いまの実装は生成シートに 1 枚も書かない**（**段が欠けたまま書くと手直しが消える** → [`shell.js`](shell.js)）。
**書きの時間を落としたまま「1 実行」と呼ばないために、空を返す仮の段を渡して書き戻しを通した回を別に測ってある**
（表の 1 行目）。**渡した仮の段は数えるものを 1 つも持たない。**
**指標の段が入れば、そのぶんの時間はここに乗る**（**指標シートに書くのは人数分の行である**）。

**どう走らせたか。** **リポジトリの 5 ファイル**（`core` ／ `shell` ／ `menu` ／ **新しく入れた** `expand` ／ `generate`）
**を貼り直してから、使い捨ての `.gs` を 1 つ足して走らせた**（**入力を置く手と時計だけを持つファイルである。
実装は 1 文字も書き換えていない**）。**貼った 5 ファイルとも SHA-256 の先頭 16 桁を突き合わせて一致を見ている。**
**使い捨ての `.gs` はリポジトリに残さない**（**確認に使うスプレッドシートごと消す** → 下の「確認に使うファイルの規約」）。

### 15: M2 を実機で判定する（[#153](https://github.com/jun-eg/school-festival-shift/issues/153)）— **見た（2026-09-22）**

**数え方そのものは手元が持つ**（[`../scripts/M2の判定.mjs`](../scripts/M2の判定.mjs)。
**違反を数える側も未充足を名指しする側も `SpreadsheetApp` を 1 度も掴まない** → 6 の #8）。
**手元の件数は [`../docs/tech-requirements.md`](../docs/tech-requirements.md) の更新ログが持つ。ここに写さない。**
**実機で新しく分かるのは、殻が実機のセルから読んだ入力で同じ件数になるかと、同じ案が出るかである**（下の節）。

| # | 何が分からないか | 外れたらどうするか | 判定を持つのはどこか | 確かめたか |
| --- | --- | --- | --- | --- |
| 15 | **殻が実機のセルから読んだ入力で、M2 の 2 つの線が手元と同じになるか**（**セルの表現が揺れれば、枠も需要も希望も変わりうる** → 6 の #8 の理由 ③）。**あわせて、同じ入力から同じ案が出るか**（→ 6 の #3 の理由 ③） | **件数が違えば、殻が揃えた表現のほうを疑う**（→ 項目 8・[`shell.js`](shell.js) の `normalizeValue`）。**案が違えば、並べ替えの鍵に入力の外のものが入っている**（→ [`generate.js`](generate.js)。**規則も方式も動かさない**） | **合格の線は [`../scripts/M2の宣言.json`](../scripts/M2の宣言.json) が持つ。**ここは実機で同じになったかだけを言う | **確かめた（2026-09-22。3 回）。どれも手元と同じだった** — **違反 0 件・名指しされていない未充足 0 件**で、**案も 1 行残らず同じ**（**SHA-256 の先頭 16 桁が一致**）。**1 回目は 1 日 1 本の帯の入力で 660 行**（下の「[M2 を実機で判定したときの件数](#m2-を実機で判定したときの件数項目-15)」）、**2 回目は確定シフトから算出した 5 時刻の入力で 681 行**（→ [#209](https://github.com/jun-eg/school-festival-shift/issues/209)。下の「[5 時刻が動いた後に判定し直した](#5-時刻が動いた後に判定し直した209)」）、**3 回目は需要に時間帯が入った後の入力で 300 行**（→ [#210](https://github.com/jun-eg/school-festival-shift/issues/210)。下の「[需要に時間帯が入った後に判定し直した](#需要に時間帯が入った後に判定し直した210)」）。**3 回目だけは [`../src/`](.) が動いているので、4 ファイルを貼り直してから走らせた** |

### 16: 手直しの数え直しを実機で走らせる（[#155](https://github.com/jun-eg/school-festival-shift/issues/155)）— **見た（2026-09-23）。外れた前提が 1 つある**

**手元で見たのは、偽のスプレッドシートの上でイベントを 1 つ渡したときだけである**（→ [`shell.test.mjs`](shell.test.mjs) の ⑦）。
**単純トリガーが本物の上で呼ばれるかは、手元では分からない。**

| # | 実機で見ること | 外れたらどうするか | 確かめたか |
| --- | --- | --- | --- |
| 16 | **マス目のセルを 1 つ書き換えると `onEdit` が走り、右下に一言が出て、検証結果・指標・色（背景は役割の色、違反した所は赤い太字）が書き直されるか。1 回に何秒かかるか**（**単純トリガーの上限は 30 秒**である）。**あわせて、続けて 2 セル書き換えたときに最後の状態で終わるか** | **走らなければ、単純トリガーで受けられない理由を見る**（インストール型にするなら **`script.scriptapp` を足すことになり、承認画面が変わる** → [`README.md`](README.md) の「手で書いた以上、自動では増えない」）。**30 秒に当たるなら、構造の検証か読む範囲を削る側を先に見る**（**数え方は動かさない**） | **確かめた（2026-09-23）。1 セルずつなら走った** — **書き換えるたびに `onEdit` が走り、右下に一言が出て、検証結果・指標・色が書き直された。1 回 7.2〜9.9 秒**（**上限 30 秒の内**）。**違反した所は赤い太字になり、背景は役割の色のままである。****続けて 2 セル書き換えると、`onEdit` は 1 回しか走らなかった**（**2 回踏んで 2 回とも**）。**数は最後の状態で終わる**（数え直しはシート全体を読む）**が、2 手目のセルには印が付かない** → 下の「[指標から画像まで 1 周通した](#指標から画像まで-1-周通した223)」 |

### 17: 手直しの印を実機で付けて、生成し直す（[#156](https://github.com/jun-eg/school-festival-shift/issues/156)）— **見た（2026-09-23）**

**手元で見たのは、偽のスプレッドシートの上でメモを読み書きしたときだけである**（→ [`shell.test.mjs`](shell.test.mjs) の ⑧）。
**印はセルのメモ（ノート）である**（→ [`README.md`](README.md) の「手直しは、生成し直しても残る」）。
**単純トリガーの中からメモが書けるか・`getNotes` がメモの無いセルに何を返すかは、手元では分からない。**

| # | 実機で見ること | 外れたらどうするか | 確かめたか |
| --- | --- | --- | --- |
| 17 | **マス目のセルを 1 つ書き換えると、そのセルにメモ「手直し — …」が付くか**（`onEdit` の中の `setNotes`）。**空にしたセルにも付くか。**そのあと**「生成」を押すと、印のセルが残り、印の無いセルが組み直され、印が付け直されるか**。**置けない手直し（例: `調理担当ですか？` が いいえ の人の枠に `調理`）が検証結果の先頭とセルのメモに出て、一言に件数が出るか**。**メモの読み書きが増えたぶん、1 実行と 1 回の数え直しが何秒になるか**（**単純トリガーの上限は 30 秒**） | **メモが付かなければ、単純トリガーから書けない理由を見る。****印の置き場を替えるなら、5-3 の「どのセルが手直しか」に戻して決め直す**（**区別できなくなる形 — 印を持たずに前の周の案全体を読む — には倒さない** → 5-3 の却下した形）。**秒が上限に近ければ、メモを読む範囲を中身のある行だけに削る側を先に見る**（**置き方は動かさない**） | **確かめた（2026-09-23）。手元と同じだった** — **書き換えたセルにも空にしたセルにも、単純トリガーの中からメモが付いた。****「生成」を押し直すと、空にした印のセルは空のまま残って印が付け直され、印の無いセルは組み直された。****置けない手直し 2 件（規則 1・規則 5）は検証結果の先頭に `食い違った固定` で出て、セルのメモも `残せなかった手直し「…」— …` に替わり、一言に「2 件」と出た。****案・検証結果・指標の 3 つとも、同じ手直しを入れた手元の `build` と指紋が一致した。****1 実行 10.017 秒 ／ 1 回の数え直し 7.2〜9.9 秒。****メモの無いセルを印と取り違えたものは 0 件**（**印の件数が手で付けた数と一致した**。`getNotes` の生の返り値は見ていない — **殻が `String(note || '')` で空文字に揃えてから読む** → [`shell.js`](shell.js) の `readGrid`）。**ただし、続けて書き換えた 2 手目には印が付かない**（→ 項目 16） |

### 18: 配る画像を実機で書き出し、LINE ノートに貼る（[#157](https://github.com/jun-eg/school-festival-shift/issues/157)）— **見た（2026-09-23）。リンクでは落ちず、右クリックで保存できた**

**手元で見たのは 2 つである。** **① 図形の中身**（→ [`distribution-image.test.mjs`](distribution-image.test.mjs) ・ [`shell.test.mjs`](shell.test.mjs) の ⑨）と、
**② 本物の [`export-images.html`](export-images.html) を headless の Chromium で開き、`google.script.run` だけ偽物にして塗らせた PNG 4 枚**（2026-09-23。M2 と同じ入力から生成した案。**4 枚とも描けた**）。
**Apps Script のダイアログは別オリジンの iframe の中にある**（→ 下の「どうやってダイアログの中を走らせたか」）**ので、そこから `download` のリンクで保存が始まるかは、手元では分からない。**

| # | 実機で見ること | 外れたらどうするか | 確かめたか |
| --- | --- | --- | --- |
| 18 | **メニュー「画像を書き出す」を押すと、ダイアログに 4 枚が出て、「保存する」で PNG が 1 枚ずつ落ちるか。「4 枚とも保存する」で 4 枚とも落ちるか。**落ちた PNG を**LINE ノートに貼り、スマホで拡大して役割名が読めるか** | **リンクで落ちなければ、画像の長押し・右クリックで保存する側を手順にする**（ダイアログに一文が出ている）。**それでも保存できないなら、6 の #5 に戻す**（**外部のライブラリを読む側には倒さない**）。**読めなければ、画素の倍率（`imageScale`）か列の幅（`imageMetrics.slotWidth`）を動かす**（**載せる中身は動かさない** → 5 の #9） | **確かめた（2026-09-23）。ただし保存はリンクではなく右クリックである** — **ダイアログは開き、4 枚とも描けて、手直しの後の案が載っていた。****「保存する」も「4 枚とも保存する」も押したが、PNG は 1 枚も落ちなかった。****同じ Chrome で、最上位のページからのダウンロードは落ちる**（対照）。**サンドボックスの iframe からのダウンロードが止まっていると見ているが、切り分けてはいない。****右クリックの「名前を付けて画像を保存」では保存でき、LINE ノートに貼ってスマホで拡大すると役割名が読めた**（**人の手で見た**。→ 下の「[指標から画像まで 1 周通した](#指標から画像まで-1-周通した223)」） |

### 19: 段を差し替えずに「生成」を押す（[#154](https://github.com/jun-eg/school-festival-shift/issues/154)）— **見た（2026-09-23）**

**これまでの書き戻しは、空の指標を返す段を差し替えて見ただけである**（→ 下の「[生成シート 3 枚に書き戻した](#生成シート-3-枚に書き戻した)」・[#210](https://github.com/jun-eg/school-festival-shift/issues/210)）。
**指標の段が入ったので、差し替えずに押せる。**

| # | 実機で見ること | 外れたらどうするか | 確かめたか |
| --- | --- | --- | --- |
| 19 | **メニューの「生成」を 1 回押すと、段を差し替えずに割り当て 4 枚・検証結果・指標の 3 種が書かれるか。**指標が人ごとに `合計時間 / シフト回数 / 準備回数` で並び、**氏名が回答から入り、順位・閾値・強調が 1 つも出ないか。**案と指標が手元（M2 と同じ入力）と同じか（SHA-256 の先頭 16 桁） | **書かれなければ `builtInSteps` と `coreSteps` の食い違いを見る**（→ [#185](https://github.com/jun-eg/school-festival-shift/issues/185) の 1 と同じ所）。**数が違えば殻の読み（`normalizeValue`）を疑う。数え方（5-6）は動かさない** | **確かめた（2026-09-23）。手元と同じだった** — **1 回押して 3 種とも書かれた**（**未了 0**）。**割り当て 826 行・検証結果 48 行・指標 36 行で、3 つとも指紋が手元と一致した。****指標の和は 合計時間 413 ／ シフト回数 177 ／ 準備回数 66 で、氏名は 36 行とも入り、背景・太字・文字色・条件付き書式は 1 つも無い。****1 実行 10.117 秒**（→ 下の「[指標から画像まで 1 周通した](#指標から画像まで-1-周通した223)」） |

### 20: 続けて書き換えた 2 手目に印が付くかを実機で見る（[#226](https://github.com/jun-eg/school-festival-shift/issues/226)）— **まだ見ていない**

| # | 実機で見ること | 外れたらどうするか | 確かめたか |
| --- | --- | --- | --- |
| 20 | **「生成」を 1 回押した後、G2 → H2 のように 2 セルを間を置かずに書き換えると、H2 にも印（メモ「手直し — …」）が付くか**（`onEdit` は 1 回しか走らなくてよい。**1 回目の数え直しが H2 を読んでいれば、そこで付く**）。**付かなければ、そのまま「生成」を押して、H2 が戻らず印が付くか。****単純トリガーの中から developer metadata を読み書きできるか**（`getDeveloperMetadata` ／ `addDeveloperMetadata` ／ `setValue`）。**控えの読み書きで、1 回の数え直しと 1 実行が何秒増えるか**（**単純トリガーの上限は 30 秒**） | **developer metadata が単純トリガーから読み書きできなければ、置き場を替える側を先に見る**（**印の置き方（メモ）は動かさない** → 5-3 ／ [`ADR tech-requirements-0013`](../docs/ADR/tech-requirements/0013-取りこぼした書き換えを拾うために前に見たマス目を見えない所に控えるようになった.md)）。**控えに当たる文字数の上限で置けなければ、詰め方（[`assignment-grid.js`](assignment-grid.js) の `seenGrid`）を見る。****秒が上限に近ければ、控えを読むシートを書き換えられた 1 枚に絞る側を見る** | **まだ見ていない。****手元では通った**（[`shell.test.mjs`](shell.test.mjs) の ⑩ ／ [`../scripts/M4の判定.mjs`](../scripts/M4の判定.mjs) の 3 周が控えを通って 0） |

## M2 を実機で判定したときの件数（項目 15）

**2026-09-22。手元と同じ件数になり、案も同じだった。**
[#199](https://github.com/jun-eg/school-festival-shift/issues/199) で使ったスプレッドシート
（**項目 14 と同じもの。回答シートにモックの行が入っている**）**の上で、`src/` の実装をそのまま走らせた。**

**貼ってある 14 ファイルと [`form-picker.html`](form-picker.html) は、SHA-256 の先頭 16 桁がリポジトリ側と 1 つ残らず一致した**
（**項目 14 の回から `src/` は動いていない。貼り直していない**）。**使い捨ての `check-153.gs` を 1 つ足しただけである**
（**入力を読む手と、数えて突き合わせる手と、時計しか持たない。実装は 1 文字も書き換えていない。
規則も数え方も 1 つも書いていない** — **`countViolations` も `nameUnmet` も `requiredAt` も `readAssignments` も、
実機に貼ってあるものをそのまま呼んでいる**）。

| 何を見たか | 手元（Node） | 実機（Apps Script の V8） |
| --- | --- | --- |
| **回答の行**（殻が読んだもの） | **46** | **46** |
| **取り込んだ人数** | **36** | **36** |
| **枠**（日ごと） | **88**（26/24/24/14） | **88**（26,24,24,14） |
| **午前と午後の境目** | `12:00` | `12:00` |
| **割り当ての行** | **660** | **660** |
| **違反**（合格の線 **0 件**） | **0 件**（規則 1・3・4・5・二重とも 0） | **0 件**（同じ） |
| **未充足**（線を当てない） | **83 件・あと 115 人** | **83 件・あと 115 人** |
| **名指しされていない未充足**（合格の線 **0 件**） | **0 件** | **0 件** |
| **突き合わせ** | **704 ＝ 589 ＋ 115 ／ 超過 0 枠** | **704 ＝ 589 ＋ 115 ／ 超過 0 枠** |
| **案の指紋**（割り当て 660 行の SHA-256 先頭 16 桁） | `032bb753cfd9519d` | **`032bb753cfd9519d`** |

**分かったことは 3 つある。**

**① 殻が実機のセルから読んでも、M2 の件数は 1 つも動かなかった。**
**枠も需要も希望も手元と同じに組め、違反 0 件・名指しされていない未充足 0 件がそのまま出た。**
**項目 8 で見た「セルの表現」が、M2 の数えにも効いていないということである。**

**② 同じ入力から同じ案が出た。****660 行が 1 行も違わない**（→ 6 の #3 の理由 ③）。
**Apps Script の V8 と手元の Node で、並べ替えの鍵に入力の外のものが入っていない**ことが、実機でも立った。
**6-1 の #2 の逃げ道（生成をブラウザ側に移す）を踏んだときに確かめると書いてあるのは、これである**（→ 項目 14）。

**③ 実機のコアは、やはり手元より速い。****手元の Node が 0.78 秒**（→ 更新ログ）**なのに対し、実機は 0.443 秒 ／ 0.587 秒である**
（**項目 14 の 0.245〜0.457 秒と同じ並びである**）。**読み（`SpreadsheetApp` の往復）は 2.043 秒 ／ 2.061 秒**で、
**1 実行の時間を決めているのが往復のほうであることも変わっていない。**
**上限（360 秒）との突き合わせはここが持たない**（→ [`../scripts/生成の所要時間.mjs`](../scripts/生成の所要時間.mjs)）。

**ここで見ていないもの。** **書き戻し**（**指標の段が未了なので、殻は 1 枚も書かない** → 項目 14 の「仮の段」）。
**規則 3 の ⑥**（**上流の △ 5 待ち。実機でも数えに入っていないことを出力に出した** → 5-4 の但し書き）。

**この回のあとで、M2 の判定入力の 5 時刻が動いた**（**2026-09-22。1 日 1 本の帯 → 確定シフトから算出した日ごとの 5 時刻**
→ [`ADR tech-requirements/0007`](../docs/ADR/tech-requirements/0007-前回の5時刻を確定シフトから算出しM2の判定入力に入れた.md)）。
**上の表は、動く前の入力で見た値である。書き換えない** — **実機で何を見たかの記録だからである。**
**動いた後の入力でも、同じ日に実機で判定した**（下の「[5 時刻が動いた後に判定し直した](#5-時刻が動いた後に判定し直した209)」）。

### 5 時刻が動いた後に判定し直した（[#209](https://github.com/jun-eg/school-festival-shift/issues/209)）

**2026-09-22。手元と同じ件数になり、案も同じだった。**
**同じスプレッドシートで、条件入力の「日ごとの営業時刻」の 5 時刻だけを入れ替えた**
（**入れ替えたのは真ん中の 3 つ（`調理開始`・`調理終了`・`片付け開始`）で、`準備開始` と `片付け終了` は動かしていない**）。
**`src/` は 1 文字も動いていない。貼り直してもいない。**

**貼ってある 15 ファイルは、SHA-256 の先頭 16 桁がリポジトリ側と 1 つ残らず一致した**
（`sheet-layout` ／ `input-types` ／ `core` ／ `take-in` ／ `count-violations` ／ `name-unmet` ／ `form-definition` ／
`shell` ／ `verify-structure` ／ `build-template` ／ `build-form` ／ `menu` ／ `expand` ／ `generate` ／ `form-picker.html`）。
**`appsscript.json` だけは差が出る** — **`sheets.macros` の 3 つ**（`check199Picker` ／ `check199Path` ／ `check199Answers`）**が増えているからである**
（→ 上の「`oauthScopes` が 3 つになった後」。**3 スコープも `timeZone` も `runtimeVersion` もリポジトリ側と同じである**）。
**使い捨ての `check-209.gs` を 1 つ足しただけである**（**帯の定義も役割名も、ここには 1 つも書いていない** —
**実機に貼ってある `prepCleanupBands` ／ `isInBand` ／ `ruleRoles` をそのまま呼ぶ**）。
**M2 の 2 つの線を数えるのは、前回と同じ `check-153.gs` の `checkM2` である。1 文字も直していない。**

**殻が実機のセルから読んだ 5 時刻と、そこから刻んだ帯。**

| 日 | 読んだ 5 時刻 | 全枠 | 準備帯 | 調理帯 | 片付け帯 |
| --- | --- | --- | --- | --- | --- |
| **2025-11-01** | `08:00 21:00 21:00 21:00 21:00` | 26 | **26** | 0 | 0 |
| **2025-11-02** | `08:00 10:00 18:00 18:00 20:00` | 24 | **4** | **16** | **4** |
| **2025-11-03** | `08:00 10:00 18:00 18:00 20:00` | 24 | **4** | **16** | **4** |
| **2025-11-04** | `08:00 15:00 15:00 15:00 15:00` | 14 | **14** | 0 | 0 |

**`準備帯` と `片付け帯` は実機が `isInBand` で数えた値である**（**調理帯は残りである** — `prepCleanupBands` は準備と片付けの 2 つしか持たない）。

| 何を見たか | 手元（Node） | 実機（Apps Script の V8） |
| --- | --- | --- |
| **回答の行**（殻が読んだもの） | **46** | **46** |
| **取り込んだ人数** | **36** | **36** |
| **枠**（日ごと） | **88**（26/24/24/14） | **88**（26,24,24,14） |
| **午前と午後の境目** | `12:00` | `12:00` |
| **割り当ての行** | **681** | **681** |
| **`準備` の割り当て** | **24 件** | **24 件** |
| **`片付け` の割り当て** | **26 件** | **26 件** |
| **違反**（合格の線 **0 件**） | **0 件**（規則 1・3・4・5・二重とも 0） | **0 件**（同じ） |
| **未充足**（線を当てない） | **59 件・あと 73 人** | **59 件・あと 73 人** |
| **名指しされていない未充足**（合格の線 **0 件**） | **0 件** | **0 件** |
| **突き合わせ** | **704 ＝ 631 ＋ 73 ／ 超過 0 枠** | **704 ＝ 631 ＋ 73 ／ 超過 0 枠** |
| **案の指紋**（割り当て 681 行の SHA-256 先頭 16 桁） | `3170caaab9ef923c` | **`3170caaab9ef923c`** |

**役割ごとの件数も実機と手元で 1 つも違わない** —
**調理責任者 86 ／ 調理 176 ／ 呼び込み 170 ／ 会計 60 ／ 列整理 139 ／ 準備 24 ／ 片付け 26** である。

**分かったことが 2 つある。**

**① 規則 3 の ② が実機でも発火した。** **前の回は `準備` が 0 件だった**（**1 本の帯では準備帯が 0 枠だった** → 上の表の回）。
**帯が立った入力では、実機でも `準備` が 24 件出る。****実機で数えても違反は 0 件のままである。**

**② 秒は前の回と変わらない。** **読み（`SpreadsheetApp` の往復）2.041 秒 ／ コア `build` 1 周 0.387 秒 ／ 読み＋コア 2.428 秒。**
**1 実行の時間を決めているのが往復のほうであることも変わっていない**（→ 項目 14 と同じ並び）。
**上限（360 秒）との突き合わせはここが持たない**（→ [`../scripts/生成の所要時間.mjs`](../scripts/生成の所要時間.mjs)）。

**ここで見ていないもの。** **書き戻し**（**`checkM2` も `check209Bands` も `build` を直に呼ぶので、シートに 1 枚も書いていない**）。
**規則 3 の ⑥**（**上流の △ 5 待ち**）。**5 時刻の算出そのもの**（**算出するのは手元である** →
[`../scripts/前回の5時刻.mjs`](../scripts/前回の5時刻.mjs)。**実機に入れたのは、算出した値を条件入力に書いたものである**）。
**崩したシートで名指しして止まるか**（**崩す操作をしていない**。項目 14 と同じ）。

### 需要に時間帯が入った後に判定し直した（[#210](https://github.com/jun-eg/school-festival-shift/issues/210)）

**2026-09-22。手元と同じ件数になり、案も同じだった。**
**同じスプレッドシートで、[`../src/`](.) の 4 ファイルを貼り直し、条件入力に「委員会の指定枠」の 2 行を足した。**

**前の 2 回と違って、今回は [`../src/`](.) が動いている。**
**貼り直したのは [`sheet-layout.js`](sheet-layout.js) ／ [`input-types.js`](input-types.js) ／ [`name-unmet.js`](name-unmet.js) ／ [`generate.js`](generate.js) の 4 つ**
（**時間帯を空けた需要が効く先が「全枠」から「その日の `調理開始`〜`調理終了` の帯」に変わった** →
[`ADR tech-requirements/0008`](../docs/ADR/tech-requirements/0008-時間帯を空けた需要が効くのは全枠ではなくその日の調理帯になった.md)）。
**貼る前に、載っていた 15 ファイルが 1 つ残らず `main` と一致していることを SHA-256 の先頭 16 桁で確かめ、
貼った後に、15 ファイルとも今度はリポジトリの作業ブランチと一致することを同じ手で確かめた**
（**保存が本当にサーバへ載ったかを見るために、エディタを開き直してから測り直している**）。
**[`appsscript.json`](appsscript.json) だけは差が出る** — **`sheets.macros` の 3 つが増えているからである**（→ 前の回と同じ）。

**使い捨ての `check-153.gs` を 1 行だけ直した。** **前の回は「1 文字も直していない」と書いたが、今回は直している。**
**直したのは `requiredAt(needs, day.date, slot, role)` を `requiredAt(needs, day, slot, role)` にする 1 行だけ**である
（**空欄の時間帯をその日の調理帯と読むには、日付の文字列ではなく日そのものが要る**。
**手元の [`../scripts/M2の判定.mjs`](../scripts/M2の判定.mjs) で直したのと同じ 1 行で、数え方も線も 1 つも動かしていない**）。

**条件入力に足したのは「委員会の指定枠」の 2 行だけである**（`2025-11-02 16:00-17:00` ／ `2025-11-03 12:00-12:40` の
`クリーンパトロール` 各 3 人）。**「日ごとの営業時刻」も「役割と必要人数」も 1 セルも動かしていない** —
**必要人数は 5 行とも日も時間帯も空欄のままである。**

| 何を見たか | 手元（Node） | 実機（Apps Script の V8） |
| --- | --- | --- |
| **組む人数** | **36** | **36** |
| **枠**（日ごと） | **88**（26/24/24/14） | **88**（26,24,24,14） |
| **午前と午後の境目** | `12:00` | `12:00` |
| **委員会の指定枠** | **2 行** | **2 行** |
| **割り当ての行** | **300** | **300** |
| **違反**（合格の線 **0 件**） | **0 件**（規則 1・3・4・5・二重とも 0） | **0 件**（同じ） |
| **未充足**（線を当てない） | **2 件・あと 2 人** | **2 件・あと 2 人** |
| **名指しされていない未充足**（合格の線 **0 件**） | **0 件** | **0 件** |
| **突き合わせ** | **268 ＝ 266 ＋ 2 ／ 超過 0 枠** | **268 ＝ 266 ＋ 2 ／ 超過 0 枠** |
| **案の指紋**（割り当て 300 行の SHA-256 先頭 16 桁） | `badd39820c91bf47` | **`badd39820c91bf47`** |

**分かったことが 3 つある。**

**① 需要のべが実機でも 268 だった。** **殻が実機のセルから読んだ入力で、空欄の 5 行が「その日の調理帯」に効いている。**
**「全枠に効く」のままなら 704 になるので、この 1 つの数で読みが変わったことが実機の側からも見える。**
**突き合わせ（`需要のべ ＝ 置いた ＋ あと何人`）が揃っているので、生成の側と数える側が同じ需要の上に乗っていることも変わっていない。**

**② `クリーンパトロール` が実機でも 12 人分立った。** **規則 6 がこの経路で初めて試された**
（**前の 2 回は「委員会の指定枠」が空だった**）。
**`12:00`-`12:40` が 30 分枠の境目に乗らない指定枠で、`12:00` と `12:30` の 2 枠に効いている** —
**のべ 12 ＝ 2 枠 × 3 人 × 2 日 がそれを示している**（→ 3 の境界値の表）。

**③ 同じ入力から同じ案が出た。** **300 行が 1 行も違わない**（→ 6 の #3 の理由 ③）。
**`src/` が動いた後でも、並べ替えの鍵に入力の外のものが入っていない。**
**役割ごとの件数を実機で別に数えてはいない** — **指紋が一致しているので、行そのものが同じである。**
**手元の内訳は 調理責任者 32 ／ 調理 64 ／ 会計 30 ／ 呼び込み 64 ／ 列整理 64 ／ クリーンパトロール 12 ／ 準備 13 ／ 片付け 21 である。**

**秒は前の回と並びが変わらない。** **読み（`SpreadsheetApp` の往復）3.190 秒 ／ コア `build` 1 周 0.403 秒 ／ 読み＋コア 3.593 秒。**
**1 実行の時間を決めているのが往復のほうであることも変わっていない**（→ 項目 14 と同じ並び）。
**上限（360 秒）との突き合わせはここが持たない**（→ [`../scripts/生成の所要時間.mjs`](../scripts/生成の所要時間.mjs)）。

**ここで見ていないもの。** **規則 3 の ⑥**（**上流の △ 5 待ち**）。**未充足がどの日のどの帯に出ているか**
（**実機が出すのは件数と突き合わせだけである。帯ごとの分布は手元で数えた** →
[`../docs/tech-requirements.md`](../docs/tech-requirements.md) の更新ログ）。
**崩したシートで名指しして止まるか**（**崩す操作をしていない**。前の回と同じ）。

#### 生成シート 3 枚に書き戻した

**上の判定は `build` を直に呼んでいるので、シートに 1 枚も書いていない。****続けて、殻の `run` で書き戻した。**

**書き戻しは、いまの実装のままでは走らない。** **`指標を出す` が未了なので（→ [#154](https://github.com/jun-eg/school-festival-shift/issues/154)）、
殻は 1 枚も書かない**（→ [`shell.js`](shell.js) の `writeOutputs`。**空の配列で上書きすると担当者の手直しが黙って消える** → 5-3）。
**段を差し替える口**（[`core.js`](core.js) の `build` の `steps`。**手元の検査が使っているのと同じ口である** → [`core.test.mjs`](core.test.mjs) の ④）
**に、空の指標を返す段を 1 つ渡して走らせた。****使い捨ての `check-210.gs` はそれだけを書いている** —
**規則も数え方も書き戻し方も 1 つも持たず、実機に貼ってある `run` をそのまま呼ぶ。**

| シート | 入った行 | 中身 |
| --- | --- | --- |
| **割り当て** | **300 行** | **役割ごとに 準備 13 ／ 調理責任者 32 ／ 調理 64 ／ 会計 30 ／ 呼び込み 64 ／ 列整理 64 ／ クリーンパトロール 12 ／ 片付け 21**（**手元と 1 つも違わない**）。**`氏名` は空である**（→ 5-5「氏名を埋めない」） |
| **検証結果** | **2 行** | **どちらも `未充足`**（`2025-11-02` の `16:00`-`16:30` と `16:30`-`17:00` の `会計` が **あと 1 人**ずつ）。**`違反` の行は 1 つも無い** |
| **指標** | **0 行**（見出しだけ） | **段が未了だからである**（→ [#154](https://github.com/jun-eg/school-festival-shift/issues/154)）。**空の段を渡したので、殻は見出しの下を消して何も書かない** |

**保護のかかった 2 枚**（`検証結果`・`指標`）**にも、確認を出さずに書けた**（→ 上の「[数える側を入れてから走らせた](#数える側を入れてから走らせた185)」の 3 と同じ）。

**分かったことが 1 つある。** **準備日（`2025-11-01`）と片付け日（`2025-11-04`）に、割り当てが 1 行も立たない。**
**300 行は `2025-11-02` に 143 ／ `2025-11-03` に 157 で、あとの 2 日は 0 である。**
**理由は 2 つ重なっている** — **① その 2 日は調理帯が 0 枠なので、店の 5 役割の需要が立たない**（それがこの issue の決めである）。
**② 規則 3 が `準備`・`片付け` を置くのは「その日に置かれた人」についてだけなので、誰も置かれない日には 1 行も出ない**（→ 5-5）。
**違反にも未充足にもならない** — **需要が無いので未充足は数えようがなく、誰も置いていないので規則 3 も破れない。**
**`準備`・`片付け` を需要に立てないと決めた**（→ 5-1 の #2 の但し書き）**ことの、いちばん目に見える帰結である。**
**判定の線は動いていない**（違反 0 件・名指しされていない未充足 0 件のままである）。

**読み＋コア＋書き戻しで 4.790 秒**（**上限 360 秒に対して 75 倍**）。
**書き戻しのぶんが乗っても、1 実行の時間を決めているのが `SpreadsheetApp` の往復であることは変わらない**（→ 項目 14 と同じ並び）。

### 準備・片付けを需要に立てた後に判定し直した（[#210](https://github.com/jun-eg/school-festival-shift/issues/210) の続き）

**2026-09-22。手元と同じ件数になり、案も同じだった。****準備日・片付け日が埋まった。**

**上の回で書き戻したら準備日（`2025-11-01`）と片付け日（`2025-11-04`）が丸ごと空だったので、
[`../src/`](.) の 3 ファイルを貼り直し、条件入力に `準備`・`片付け` の 6 行を足して走らせ直した**
（→ [`ADR tech-requirements/0009`](../docs/ADR/tech-requirements/0009-準備と片付けを需要に立て規則3の5が当たらなくなった.md)）。
**貼り直したのは [`count-violations.js`](count-violations.js) ／ [`name-unmet.js`](name-unmet.js) ／ [`generate.js`](generate.js) で、
3 つとも SHA-256 の先頭 16 桁をリポジトリ側と突き合わせてから保存した**（**保存後にエディタを開き直して測り直している**）。
**足した 6 行は、日を指定して時間帯を空けたものである**
（`2025-11-01` `準備` 16 ／ `2025-11-02` `準備` 8・`片付け` 15 ／ `2025-11-03` `準備` 8・`片付け` 26 ／ `2025-11-04` `準備` 9）。
**時間帯は 1 つも書いていない** — **空欄がその役割の帯に効くからである。**
**`check-153.gs` も `check-210.gs` も 1 文字も直していない。**

| 何を見たか | 手元（Node） | 実機（Apps Script の V8） |
| --- | --- | --- |
| **組む人数** | **36** | **36** |
| **枠**（日ごと） | **88**（26/24/24/14） | **88**（26,24,24,14） |
| **割り当ての行** | **826** | **826** |
| **違反**（合格の線 **0 件**） | **0 件**（規則 1・3・4・5・二重とも 0） | **0 件**（同じ） |
| **未充足**（線を当てない） | **48 件・あと 212 人** | **48 件・あと 212 人** |
| **名指しされていない未充足**（合格の線 **0 件**） | **0 件** | **0 件** |
| **突き合わせ** | **1038 ＝ 826 ＋ 212 ／ 超過 0 枠** | **1038 ＝ 826 ＋ 212 ／ 超過 0 枠** |
| **案の指紋**（割り当て 826 行の SHA-256 先頭 16 桁） | `08344ffd8a724630` | **`08344ffd8a724630`** |

**書き戻した 3 枚。** **割り当て 826 行 ／ 検証結果 48 行（どれも `未充足`。`違反` は 0 行）／ 指標 0 行**である。
**役割ごとは 準備 482 ／ 調理責任者 32 ／ 調理 64 ／ 会計 30 ／ 呼び込み 64 ／ 列整理 64 ／ クリーンパトロール 12 ／ 片付け 78。**
**割り当てシートの先頭が `2025-11-01` の `準備` になった** — **前の回は `2025-11-02` から始まっていた。**

**分かったことが 2 つある。**

**① 準備日・片付け日が埋まった。** **`2025-11-01` が 0 → 342 行、`2025-11-04` が 0 → 88 行である。**
**`準備` は 13 件 → 482 件、`片付け` は 21 件 → 78 件。**
**規則 3 の ⑤ を当てないようにしても、実機で違反は 0 件のままだった** — **①〜④ は動いていない。**

**② 需要が 4 倍近くになっても、秒の並びは変わらない。**
**読み（`SpreadsheetApp` の往復）2.217 秒 ／ コア `build` 1 周 0.229 秒 ／ 読み＋コア 2.446 秒。**
**書き戻しまで含めた 1 実行は 6.399 秒**（**上限 360 秒に対して 56 倍**。**前の回は 4.790 秒 ／ 75 倍**）。
**1 実行の時間を決めているのが往復のほうであることは変わっていない** — **コアはむしろ前の回（0.403 秒）より速い。**

**ここで見ていないもの。** **規則 3 の ⑥**（**上流の △ 5 待ち**）。**準備・片付けの人数が妥当か**
（**記録の日ごとの中央値を置いただけで、要る人数の裏付けは無い** → [`../scripts/M2の宣言.json`](../scripts/M2の宣言.json)。
**担当者が毎年入れるものである** → 5-1 の #2）。**崩したシートで名指しして止まるか**（**崩す操作をしていない**）。

### 割り当てを 4 枚のマス目にしてから走らせた（[#213](https://github.com/jun-eg/school-festival-shift/issues/213)）

**2026-09-23。手元と 1 行ずつ同じだった。****マス目に書いて、そのまま読み戻して、同じ 826 行に戻った。**

**[`../src/`](.) の 8 ファイルを載せた** — **7 つは貼り直し**（[`sheet-layout.js`](sheet-layout.js) ／ [`input-types.js`](input-types.js) ／
[`core.js`](core.js) ／ [`form-definition.js`](form-definition.js) ／ [`shell.js`](shell.js) ／
[`verify-structure.js`](verify-structure.js) ／ [`build-template.js`](build-template.js)）、
**1 つは新規**（[`assignment-grid.js`](assignment-grid.js)）である。
**8 つとも SHA-256 の先頭 16 桁をリポジトリ側と突き合わせてから保存した。**
**貼る前の 7 つが `main` と一致していることも、同じ手で見た**（**`input-types.gs` は貼り付けが 1 回空振りし、
そのときに読んだ値が `main` と一致していた** — **空振りに気づけたのは SHA を測っていたからである**）。

**`buildTemplate` は通った。****実行ログはこう出た。**

```
シート「準備日」を作った
「準備日」を 50 列に広げた（24 列足した）
「準備日」の 1 行目 1 列目から見出しを置いた
（学祭1日目 ／ 学祭2日目 ／ 片付け も同じ 3 行）
シート「検証結果」に保護をかけた（警告のみ）
シート「指標」に保護をかけた（警告のみ）
構成に無いシート「割り当て」に中身があるので、残した
8 枚のうち保護したのは 3 枚である（割り当ては日ごとの 4 枚である → issue #213）
```

**前の周の「割り当て」1 枚は消えずに残った。****黙って消さないからである**（→ [`build-template.js`](build-template.js) の `removeDefaultSheet`）。
**構造の検証は、構成に無いシートを崩れとして数えない**ので、残っていても走る。

| 何を見たか | 手元（Node） | 実機（Apps Script の V8） |
| --- | --- | --- |
| **割り当ての行** | **826** | **826** |
| **違反**（合格の線 **0 件**） | **0 件** | **0 件** |
| **未充足**（線を当てない） | **48 件・あと 212 人** | **48 件・あと 212 人** |
| **準備日** | **行 24 人 ／ 列 26 枠 ／ 埋まったセル 342** | **同じ** |
| **学祭1日目** | **行 11 人 ／ 列 24 枠 ／ 埋まったセル 176** | **同じ** |
| **学祭2日目** | **行 23 人 ／ 列 24 枠 ／ 埋まったセル 220** | **同じ** |
| **片付け** | **行 8 人 ／ 列 14 枠 ／ 埋まったセル 88** | **同じ** |
| **氏名の入った行** | **全 66 行**（24 ＋ 11 ＋ 23 ＋ 8） | **同じ** |
| **案の指紋**（割り当て 826 行の SHA-256 先頭 16 桁。**行を並べ替えてから取る**） | `e8991d683ac4c02d` | **`e8991d683ac4c02d`** |
| **マス目から読み戻した行** | **826 ／ 指紋 `e8991d683ac4c02d`** | **826 ／ 指紋 `e8991d683ac4c02d`** |
| **往復で一致したか** | **した** | **した** |

**埋まったセルの合計が 342 ＋ 176 ＋ 220 ＋ 88 ＝ 826 で、割り当ての行と同じである** —
**1 セル 1 役割なので、行とセルが 1 対 1 で対応する。**

**実機でしか分からなかったことが 1 つある。**
**時刻の見出しは、`getValues` で `Date` として返ってくる**（`Sat Dec 30 1899 08:00:00 GMT+0900`）。
**1899-12-30 を土台にした時刻のセルで、項目 8 で見た「時刻だけのセル」と同じ返り方である。**
**殻の `normalizeValue` が `HH:MM` に揃えているので、読み戻しが成立している** —
**揃える所を通さずに見出しを読むと、いまの枠と突き合わせられない。**
**画面には `8:00` と右寄せで出る** — **記録の配布物 ◎ の書き方（`8:00`）と同じである。**

**見た目も配布物 ◎ と同じ形になった。** **`学籍番号 ／ 氏名 ／ 8:00 ／ 8:30 …` の見出しに、
1 人 1 行で `準備`・`調理`・`調理責任者`・`会計`・`呼び込み`・`列整理` が並ぶ。**
**左の 2 列は固定されていて、右へ送っても誰の行かが読める。**

**秒。****読み 1.424 ／ コア `build` 1 周 0.131 ／ 書き 3.684 ／ 1 実行 5.239**
（**上限 360 秒に対して 69 倍**。前の回は 6.399 秒 ／ 56 倍）。
**書きが読みより重いのは、4 枚ぶんの範囲を消してから置いているからである。**
**コアはむしろ前の回（0.229 秒）より速い。**

**2 周目も走らせた**（**時刻の見出しを左に寄せた [`shell.js`](shell.js) に貼り直してから**）。
**今度は「走らせる前にマス目から戻った行」が 826 になった** — **1 周目が書いたマス目を、2 周目が入力として読み戻している。**
**それでも出てくる案は同じで、指紋も `e8991d683ac4c02d` のままだった**（**秒は 読み 1.405 ／ コア 0.179 ／ 書き 2.455 ／ 1 実行 4.039**）。
**マス目を挟んで 1 周まわしても、割り当てが増えも減りもしない。**

**時刻の見出しは左に寄せた。****時刻のセルは既定で右寄せになる**ので、
**寄せないと見出しが自分の列ではなく右隣の列の頭に見える。**
**画面で見て確かめた** — **左に寄せると、`8:00` がその列の `準備` の真上に来る。**

**ここで見ていないもの。****役割ごとの色**（→ [#157](https://github.com/jun-eg/school-festival-shift/issues/157)）。
**崩したマス目で名指しして止まるか**（**崩す操作をしていない**）。
**手直しを入れてから再実行したときに、その 1 手が残るか**（→ [#156](https://github.com/jun-eg/school-festival-shift/issues/156)。
**いまの生成は固定を 1 行も見ない**ので、入れても次の周で上書きされる）。
**前の周の「割り当て」1 枚を消していない。**

### まとまりと散らしを入れてから判定し直した（[#215](https://github.com/jun-eg/school-festival-shift/issues/215)）

**2026-09-23。案が手元と 1 行も違わなかった** — **指紋が一致したので、塊も散らしも手元で数えた値がそのまま実機の値である。**

**[`../src/`](.) の 9 ファイルを貼り直した**（[`sheet-layout.js`](sheet-layout.js) ／ [`input-types.js`](input-types.js) ／
[`core.js`](core.js) ／ [`count-violations.js`](count-violations.js) ／ [`name-unmet.js`](name-unmet.js) ／
[`shell.js`](shell.js) ／ [`verify-structure.js`](verify-structure.js) ／ [`generate.js`](generate.js) ／
[`assignment-grid.js`](assignment-grid.js)）。
**貼る前の 9 つが `main` と一致していること、貼った後に作業ブランチと一致していることを、どちらも SHA-256 の先頭 16 桁で見た**
（**保存してからエディタを開き直して測り直している**）。**残る 6 つ**（[`take-in.js`](take-in.js) ／ [`expand.js`](expand.js) ／
[`form-definition.js`](form-definition.js) ／ [`build-template.js`](build-template.js) ／ [`build-form.js`](build-form.js) ／
[`menu.js`](menu.js)）**と [`form-picker.html`](form-picker.html) は、この issue で動いていないので触っていない**（**測ったら 7 つとも一致していた**）。

**条件入力に区画が 1 つ増えた。** **`buildTemplate` を走らせたら、見出しだけが増えた。**

```
「条件入力」の 1 行目 25 列目から見出しを置いた
「条件入力」の 2 行目 25 列目から見出しを置いた
シート「回答」に保護をかけた（警告のみ）
シート「検証結果」に保護をかけた（警告のみ）
シート「指標」に保護をかけた（警告のみ）
構成に無いシート「割り当て」に中身があるので、残した
8 枚のうち保護したのは 3 枚である（割り当ては日ごとの 4 枚である → issue #213）
```

**ほかの区画は 1 つも動いていない** — **`replaceValues` は「いまの値と構成が同じなら何もしない／空なら置く／違えば止まる」なので、
増えた 1 区画ぶんだけが「置いた」に出る**（→ [`build-template.js`](build-template.js)）。

| 何を見たか | 手元（Node） | 実機（Apps Script の V8） |
| --- | --- | --- |
| **型 #7 の `minRun`** | **60 分**（枠 2 つぶん） | **60 分**（枠 2 つぶん） |
| **割り当ての行** | **826** | **826** |
| **日ごと** | **342 ／ 176 ／ 220 ／ 88** | **同じ** |
| **役割ごと** | `準備` 482 ／ `調理責任者` 32 ／ `調理` 64 ／ `会計` 30 ／ `呼び込み` 64 ／ `列整理` 64 ／ `クリーンパトロール` 12 ／ `片付け` 78 | **同じ** |
| **違反**（合格の線 **0 件**） | **0 件** | **0 件** |
| **未充足**（線を当てない） | **48 件・あと 212 人** | **48 件・あと 212 人** |
| **案の指紋**（割り当て 826 行の SHA-256 先頭 16 桁。**行をタブで繋ぎ、並べ替えてから改行で繋ぐ**） | `12384a47f42c4d80` | **`12384a47f42c4d80`** |

**塊と散らしは、実機では数え直していない。****数え直す必要が無いからである** —
**案の指紋が一致した以上、その案から出る塊（1 時間未満が 2 件・どちらも `規則 3 の端`）も散らし（置けるのに置かれない人が 4 日とも 0 人）も同じ値である。**
**数え方を実機の側に書き写すと、[`scripts/まとまりと散らし.mjs`](../scripts/まとまりと散らし.mjs) と 2 通りになる**（→ [`src/README.md`](README.md)）。

**実機でしか分からなかったことが 1 つある。**
**「連続して入る最小の長さ」のセルは、`getValues` で `Date` として返ってくる。**

```
置き方のルール（生のセル）: ["連続して入る最小の長さ","1899-12-29T16:00:00.000Z"] / 値の型: [object Date]
殻が読んだ行: [["連続して入る最小の長さ","01:00"]]
型 #7 の minRun: 60 分（枠 2 つぶん）
```

**担当者が `1:00` と書いても、セルに入るのは時刻である**（`1899-12-30 01:00`。項目 8 で見た「時刻だけのセル」と同じ返り方で、
**マス目の時刻の見出しが `Date` で返るのと同じ土台である** → 上の「割り当てを 4 枚のマス目にしてから走らせた」）。
**殻の `normalizeValue` が `01:00` に揃えるので、`readRunLength` の `^\d{1,2}:[0-5]\d$` がそのまま通る。**
**桁を 2 桁に決め打っていたら、実機だけ通って手元と読みが変わる／逆に書いたとおりの 1 桁だけを通していたら、実機で落ちていた** —
**1〜2 桁のどちらも通す形にしてあったので、往復が成立した。**

**分かれ道も実機で踏んだ。**

| 入れた値 | 実機の出方 |
| --- | --- |
| **`1:00`**（`項目` も書いた） | **60 分で走り、826 行・違反 0 件・指紋一致**。**3 回走らせて 3 回とも同じ指紋である**（→ 6 の #3 の理由 ③） |
| **`1:20`** | **名指しして止まった** — 「**「置き方のルール」の 1 行目（シートの 3 行目）の「値」の「01:20」が 30 分の倍数でない。枠の刻みが 30 分なので、倍数でない長さは枠に乗らない（→ 規則 1 の ①・5-1 の #7）**」。**揃えた後の `01:20` が文に出る**ので、担当者は自分が書いた `1:20` とセルの中身の両方をたどれる |
| **`項目` が空で `値` だけ** | **名指しして止まった** — 「**「置き方のルール」の 1 行目（シートの 3 行目）の「項目」が空である**」。**行が空でないので既定にならない**（→ `eachFilledRow`）。**既定で走るのは区画に 1 行も無いときだけである** |

**秒。****読み 4.557〜4.755 ／ コア `build` 1 周 0.078〜0.199**（**3 回ぶん**）。
**コアは前の回（0.131 秒）と同じ桁である** — **まとまりで伸ばす手が増えても、実機の秒は読みの往復で決まっている。**
**上限（360 秒）に対して、コアだけなら 1800 倍以上の余裕がある。**

**続けて、生成シートに書き戻した。****殻の `run` をそのまま呼んでいる**（**「指標を出す」は未了なので、空を返す段を 1 つ渡した** —
**渡さないと殻は 1 枚も書かない** → [`shell.js`](shell.js) の `writeOutputs`・5-3。**前の回と同じ手である**）。
**未了の段は「なし」と出た**（渡した 1 つで埋まるからである）。

| 何を見たか | 手元（Node） | 実機（Apps Script の V8） |
| --- | --- | --- |
| **準備日** | **行 24 人 ／ 列 26 枠 ／ 埋まったセル 342** | **同じ** |
| **学祭1日目** | **行 11 人 ／ 列 24 枠 ／ 埋まったセル 176** | **同じ** |
| **学祭2日目** | **行 23 人 ／ 列 24 枠 ／ 埋まったセル 220** | **同じ** |
| **片付け** | **行 8 人 ／ 列 14 枠 ／ 埋まったセル 88** | **同じ** |
| **埋まったセルの合計** | **826** | **826** |
| **マス目から読み戻した行** | **826 ／ 指紋 `12384a47f42c4d80`** | **826 ／ 指紋 `12384a47f42c4d80`** |
| **検証結果 ／ 指標** | **48 行 ／ 0 行** | **48 行 ／ 0 行** |

**埋まったセルの合計が割り当ての行と同じ（826）で、書いたマス目をそのまま読み戻すと同じ 826 行・同じ指紋に戻る。**
**マス目を挟んでも、案が増えも減りもしない**（→ 上の「割り当てを 4 枚のマス目にしてから走らせた」と同じ見方である）。

**まとまりは、シートの上で目に見える。** **行を横に読むと、役割が 2 枠ずつ並ぶ** —
**`学祭1日目` の `10:00`〜`13:30` は、`調理` `調理` ／ `列整理` `列整理` ／ `調理` `調理` ／ `列整理` `列整理` のように
同じ役割が隣り合って続く**（**[issue #215](https://github.com/jun-eg/school-festival-shift/issues/215) の本文が引いている「30 分ごとに持ち場を変えている」行が、
1 時間ずつの塊になった**）。**画像はリポジトリに入れない** — **学籍番号と氏名が写るためである**（→ 下の「確認に使うファイルの規約」）。

**1 実行は 8.227 秒**（**構造の検証 ＋ 読み ＋ コア ＋ 書き**。**上限 360 秒に対して 43 倍**）。
**前の回（#213）は 5.239 秒で、書く枚数も行数も同じである** — **差は実行のたびの往復のぶれの範囲で、案の側では説明しない。**

**ここで見ていないもの。**
**区画を 1 つ増やした後の構造の検証を、崩して確かめてはいない**（**崩さずに 1 周通るほうは、`checkStructure` が毎回通っている**）。
**「置き方のルール」を区画ごと空にしたときに既定の 1 時間で走るか**（**手元の検査では見ている** → [`input-types.test.mjs`](input-types.test.mjs)。
**実機では `項目` だけを空にした形しか踏んでいない**）。

**使い捨ての `check-215.gs` と `put-215.gs` を 2 つ足した。****リポジトリには入れない**（→ 下の「確認に使うファイルの規約」）。
**`put-215.gs` は、画面からセルに貼れなかったときの逃げ道である** — **項目の名前は型の側（`placementItems`）から取っていて、この手が文字列を持たない。**

## 指標から画像まで 1 周通した（[#223](https://github.com/jun-eg/school-festival-shift/issues/223)）

**2026-09-23。** [#154](https://github.com/jun-eg/school-festival-shift/issues/154)〜[#157](https://github.com/jun-eg/school-festival-shift/issues/157) の 4 本は、**4 本とも「実機では確かめていない」で閉じている。**
**同じスプレッドシートの上で、生成 → 書き換え → 印 → 生成し直し → 画像 の順に 1 周通した回である。**
**指標・違反・固定の置き方そのものは数え直さない**（→ 上の「[手元で分かる範囲と、実機でしか分からない範囲](#手元で分かる範囲と実機でしか分からない範囲)」）。
**ここに書くのは、殻の読み書きが実機で手元と同じになったかと、実機でしか出なかった差だけである。**

### 貼り直したもの — 10 ファイル

**[#215](https://github.com/jun-eg/school-festival-shift/issues/215) の回と同じスプレッドシートである。**
**貼る前に、載っていたファイルを 1 つ残らず SHA-256 の先頭 16 桁で測った。**
**9 つは `main` と一致し、7 つ**（`sheet-layout` ／ `input-types` ／ `core` ／ `shell` ／ `menu` ／ `generate` ／ `assignment-grid`）**は #215 の回に貼った版と一致した**
（`menu` は `e7f87d5`、残りの 6 つは `2cc2101` の版である）。**実機は #215 を取り込んだ時点の `main` のままだった。**

**貼ったのは 7 つの貼り直しと、3 つの新規**（[`fairness-metrics.js`](fairness-metrics.js) ／ [`distribution-image.js`](distribution-image.js) ／ [`export-images.html`](export-images.html)）**である。**
**貼った後にエディタを開き直して測り直し、`.js` 17 ＋ HTML 2 が 1 つ残らず `main`（`754b210`）と一致した。**
**[`appsscript.json`](appsscript.json) だけは `sheets.macros` の 3 つのぶん差が出る**（→ 前の回と同じ）。
**`distribution-image` は名前の入力を取りこぼして `無題.gs` で作られた**ので、中身を貼ってから「名前を変更」で直した（**一覧の名前と SHA の両方で見ている**）。

**[`README.md`](README.md) の手順 3 は、この 17 本に直した。** **「`.js` 13 つ」と書いて 14 個挙げ、`expand` ／ `generate` ／ `fairness-metrics` が抜けていた** — **一覧どおりに貼ると 3 つ貼り忘れる。**

**使い捨ての `.gs` を 2 つ足した。** **`check-223.gs`** は 3 枚とマス目を読んで、指紋・メモ・色を出すだけである。
**`check-223-cells.gs`** は手直しに使うセルの候補（希望の外の枠 ／ 調理担当が「はい」でない人の枠）を探すだけである。
**どちらも規則も数え方も持たず、シートに 1 セルも書かない** — **実機に貼ってある `readInputsAndGrids` ／ `takeIn` ／ `expand` ／ `isFixedNote` などをそのまま呼ぶ。**

### 19 — 1 回押して、3 種とも書かれた

**メニュー「シフト」→「生成」を 1 回押した。段は差し替えていない。****未了 0 で、割り当て 4 枚・検証結果・指標の 3 種とも書かれた。**

| 何を見たか | 手元（Node。M2 と同じ入力） | 実機（Apps Script の V8） |
| --- | --- | --- |
| **割り当て**（マス目から読み戻した行） | **826 ／ `12384a47f42c4d80`** | **同じ** |
| **検証結果** | **48 行**（**全部 `未充足`**）**／ `a98c4caf693eaa59`** | **同じ** |
| **指標**（**氏名の列を空にしてから取る**） | **36 行 ／ `96ca9aa72973e3a8`** | **同じ** |
| **指標の列** | `学籍番号 / 氏名 / 合計時間 / シフト回数 / 準備回数` | **同じ** |
| **指標の和**（合計時間 ／ シフト回数 ／ 準備回数） | **413 ／ 177 ／ 66** | **同じ** |
| **氏名の入った行** | —（**手元は殻を通らないので空である**） | **36 行とも入った**（**回答から引いている** → 5 の #1） |
| **指標の見た目** | — | **背景は `#ffffff` だけ・太さは `normal` だけ・文字色は `#000000` だけ・条件付き書式 0** — **順位・閾値・強調は 1 つも出ていない** |
| **マス目の色** | — | **役割ごとの背景**（`調理` `#ffe599` ／ `調理責任者` `#f9cb9c` ／ `会計` `#9fc5e8` ／ `呼び込み` `#d5a6bd` ／ `列整理` `#b4a7d6` ／ `クリーンパトロール` `#b6d7a8` ／ `準備`・`片付け` `#d9d9d9`）。**赤い太字は 0 セル**（違反 0 件である） |

**割り当ての指紋は #215 の回（`12384a47f42c4d80`）と同じである** — **手直しが 1 つも無いとき、#154〜#157 は置き方を動かしていない。**
**1 実行 10.117 秒**（**上限 360 秒に対して 35 倍**。**#215 の回は 8.227 秒**で、**今回は指標・色・メモまで書く回である。内訳は測っていない**）。

### 16・17 — 書き換え → 印 → 生成し直し

**使ったのは `学祭1日目`（`2025-11-02`）の 1 行目の人である**（**モックの学籍番号である** → 下の「確認に使うファイルの規約」）。
**セルは `check-223-cells.gs` が探した候補から選んだ** — **C2（`08:00`）はその人の希望の外、I2（`11:00`）は希望の内で、その人の `調理担当ですか？` は「はい」でない。**

| # | 手 | 実機の出方 | `onEdit` |
| --- | --- | --- | --- |
| 1 | **C2 に `会計`** | **赤い太字になり、メモ「手直し — 生成し直しても残る（このメモを消すと、次の生成で組み直す）」が付いた。**右下に「**数え直した — 違反 1 件 ／ 未充足 48 件（検証結果と指標を書き換えた。違反した所はマス目の色で出ている）**」 | **9.738 秒** |
| 2 | **G2・H2（`10:00`・`10:30` の `会計`）を続けて空に** | **右下は「違反 1 件 ／ 未充足 50 件」** — **2 セルとも空になったあとの数である。****背景は白に戻った。****印は G2 にだけ付き、H2 には付かなかった**（→ 下の「実機でしか出なかった差」） | **1 回だけ**（**7.229 秒**） |
| 3 | **I2 に `調理`** | **赤い太字・メモが付き、「違反 2 件 ／ 未充足 50 件」** | **9.863 秒** |
| 4 | **メニュー「生成」** | 「**生成した（割り当て・検証結果・指標を書き換えた。手直しの印があるセルは残した）。残せなかった手直しが 2 件ある — 検証結果の先頭と、そのセルのメモに理由がある**」 | —（`runGeneration` **10.017 秒**） |

**「生成」を押す前**（3 の後）**の検証結果は 52 行**で、**先頭に `違反` が 2 行**（`規則 1: 希望の時間の外に置いている` ／ `規則 5: 調理の枠（調理）に置いているが、調理担当ですか？ が いいえ である`）、**残り 50 行が `未充足`** だった。
**指標も書き直された**（**シフト回数の和 177 → 178**）。**印は 3 件**（C2 `会計` ／ G2 空 ／ I2 `調理`）である。

**「生成」を押した後。**

| セル | 押す前 | 押した後 |
| --- | --- | --- |
| **C2**（`08:00`） | `会計`（印） | **空。**メモが「**残せなかった手直し「会計」— 規則 1: 希望の時間の外である**」に替わった |
| **G2**（`10:00`） | 空（印） | **空のまま。印が付け直された** |
| **H2**（`10:30`） | 空（**印なし**） | **`会計`** — **印が無いので組み直された** |
| **I2**（`11:00`） | `調理`（印） | **`会計`**（**生成が置いた**）。メモが「**残せなかった手直し「調理」— 規則 5: 調理の枠（調理）だが、調理担当ですか？ が いいえ である**」に替わった |

**検証結果は 50 行 ＝ 先頭に `食い違った固定` 2 行 ＋ `未充足` 48 行で、`違反` は 0 行に戻った。**
**食い違ったメモは頭が「手直し」でないので、次の周では印として読まれない**（→ [`assignment-grid.js`](assignment-grid.js) の `conflictNote`）。**読み直すと印は G2 の 1 件だけだった。**

**手元の `build` に、同じ手直し 3 行を入れて突き合わせた。**

| 何を見たか | 手元（Node） | 実機（Apps Script の V8） |
| --- | --- | --- |
| **割り当て** | **826 ／ `e5446757a519c0e3`** | **同じ** |
| **検証結果** | **50 行**（`食い違った固定` 2 ／ `未充足` 48）**／ `02546b2ddbb379af`** | **同じ** |
| **指標**（氏名の列を空にしてから取る） | **36 行 ／ `204aabf1d5ba67a0`** ／ 和 **413 ／ 179 ／ 66** | **同じ** |

**印を読んで生成し直しても、案は手元と 1 行も違わない。****殻がマス目とメモから組んだ手直しの入力が、手元で組んだ 3 行と同じだったということである。**

### 実機でしか出なかった差 — 続けて書き換えると、2 手目に印が付かない

**2 セルを間を置かずに書き換えると、`onEdit` は 1 回しか走らなかった。****2 回踏んで、2 回とも同じだった。**

| 踏んだ所 | 書き換えた間隔 | 実行数に出た `onEdit` | 印 |
| --- | --- | --- | --- |
| **G2 → H2**（`会計` を空に） | **1 秒未満**（クリック → Delete → クリック → Delete） | **1 回**（5:33:23 ／ 7.229 秒） | **G2 だけ** |
| **G12 → H12**（`呼び込み` を空に） | **同じ** | **1 回**（5:41:08 ／ 8.725 秒） | **G12 だけ** |

**値はどちらのセルも書き換わっている。****数え直しの数も最後の状態である**（**数え直しはシート全体を読むので、1 回走れば 2 セルとも数えに入る**）。
**落ちるのは、2 手目のセルの印だけである。****印の無いセルは次の「生成」で組み直される**ので、**担当者が続けて何セルも手直しすると、後のほうの手直しが「生成」で黙って消える**（**H2 が実際に `会計` に戻った**）。
**5-3 の「手直しが黙って消える」に当たる。**

**なぜ 1 回しか走らないかは切り分けていない。** **1 回目の `onEdit` が走っているあいだ（7 秒ほど）に来た 2 手目のイベントが落ちた**というのがいちばん素直な説明だが、
**何秒空ければ 2 回走るかは測っていない。****範囲をまとめて貼る（1 回の編集で何セルも書き換える）ときは、範囲ぜんぶに印を付ける作りである**（→ [`shell.js`](shell.js) の `markFixedCells`）が、**実機では踏んでいない。**

**直した**（→ [#226](https://github.com/jun-eg/school-festival-shift/issues/226)。**次の数え直しか生成が、控えと違うセルに印を付ける**。実機では項目 20 で見る）。
**直す先はこの文書ではない。****項目 16 の「外れたらどうするか」**（**単純トリガーで受けられない理由を見る。インストール型にするなら `script.scriptapp` を足すことになり、承認画面が変わる**）**の分かれ道に乗る判断なので、別の issue で決める。**
**この回は判断を動かさずに、見たものを書いただけである。**

### 18 — 4 枚描けた。リンクでは落ちず、右クリックで保存でき、LINE ノートで読めた

**メニュー「シフト」→「画像を書き出す」で、本物のダイアログ「画像を書き出す」が開いた。**
**「割り当ての 4 枚を読んでいる…」のあと、「4 枚描いた。保存して、LINE ノートに貼る。保存が始まらないときは、画像を右クリック（スマホは長押し）して保存する。」と「4 枚とも保存する」が出た。**
**この文は 4 日とも PNG（`canvas.toDataURL`）にできたときにだけ出る**（→ [`export-images.html`](export-images.html)）。

**4 枚とも画面で見た。****手直しの後の案が載っている** — **`11月2日（学祭1日目）シフト表` の 1 行目の人は `08:00` 空・`10:00` 空・`10:30` と `11:00` が `会計` で、マス目と同じである**
（**C2 は置けない手直しで空、G2 は印のある空、H2・I2 は生成が置いた `会計`** → 上の「16・17」）。**12 行目の人の `10:00`・`10:30` も、再現を確かめたときに空にしたとおり空である。**
**役割の色もマス目と同じ並びで、`クリーンパトロール` の緑・`片付け` の灰色まで載っている。**
**1 回目に開いたときはホイールが中に届かなかった**（**ブラウザのウィンドウが裏に回っていた** → 下）が、**前に戻ってからは届いた。**

**保存は始まらなかった。**（**ダウンロードは人の許可を取ってから押した**）

| 押したもの | Windows の「ダウンロード」フォルダ |
| --- | --- |
| **「保存する（11月1日シフト表.png）」**（リンク） | **増えない** |
| **「保存する（11月2日シフト表.png）」**（リンク。2 回押した） | **増えない** |
| **「4 枚とも保存する」**（ボタン。**ページが 4 本のリンクを順に押す作り**） | **増えない** |
| **対照: スプレッドシートの最上位のページから、`data:` の小さなテキストを `download` リンクで落とす** | **落ちた**（12 バイト。**確かめて消した**） |

**この Chrome でダウンロードそのものは動いている。****止まっているのは、ダイアログの中（別オリジンのサンドボックスの iframe）から始めたときだけである。**
**Chrome は `allow-downloads` の無いサンドボックスの iframe からのダウンロードを黙って止める**ので、**Apps Script のダイアログの iframe にそれが付いていない**というのがいちばん素直な説明である。
**それでも切り分けてはいない** — **中の iframe の `sandbox` 属性は親のページから読めず**（外側の iframe には `sandbox` が無い）、**コンソールにも理由は出てこなかった。**
**クリックが中まで届いたかも、画面に変化が出ないので見分けていない**（**ホイールは届いている**）。

**項目 18 の「外れたらどうするか」の 1 つめに当たる** — **「リンクで落ちなければ、画像の長押し・右クリックで保存する側を手順にする」**（**ダイアログにはその一文がすでに出ている**）。
**右クリックのメニューは Chrome の UI で、ブラウザ操作の側から押せないので、人の手で踏んだ — 保存できた**（下の表）。**6 の #5 に戻す必要は無い。**
**判断は動かしていない。**

**残りの 2 つは人の手で見た**（jun-eg。**Chrome のメニューも LINE も、ブラウザ操作の側からは押せない**）。

| | 結果 |
| --- | --- |
| **右クリックの「名前を付けて画像を保存」で保存できるか** | **保存できた** |
| **LINE ノートに貼り、スマホで拡大して役割名が読めるか** | **読めた。****画素の倍率（`imageScale`）も列の幅（`imageMetrics.slotWidth`）も動かさなくてよい** |

**担当者がたどる保存の手は、ダイアログの一文（右クリック・長押し）のほうになる。****操作の数は変わらない**（**2 の一覧 9「画像を書き出す」の中の 1 手である**）。
**「保存する」「4 枚とも保存する」のリンクとボタンは、この実機では何も起こさない。****残すか外すかは手順書（[#159](https://github.com/jun-eg/school-festival-shift/issues/159)）と合わせて決める。この回は `src/` を動かしていない。**

### 実装者の側で踏んだこと（`src/` には効かない）

- **C2 の 1 回目の貼り付けで文字が化けた。****`clip.exe` に BOM の無い UTF-16LE で 2 文字だけ渡すと、UTF-16 と見なされずに化ける**（**ファイルのように長い文字列では起きていない**）。
  **化けた値でも `onEdit` は走り、セルは赤い字で出た**（5:32:10 ／ 7.2 秒。**そのときの一言は読んでいない**）。**同じセルに `会計` を貼り直したので、印も数も最後の値のものである。**
  **短い文字列は CP932 で渡すと通った**
- **I2 には、1 回だけ前の `会計` が貼られた**（**クリップボードに `調理` を載せる手が権限で止められ、前の中身がそのまま残っていた**。5:34:07 ／ 8.24 秒）。
  **ページの中で `DataTransfer` を載せた `paste` イベントを送って `調理` に書き直した**（**クリップボードを触らない**。5:34:43）
- **途中でブラウザのウィンドウが裏に回り**（`document.visibilityState` が `hidden`）、**画面の取得も実クリックも届かなくなった。****前に戻ってから 18 を続けた**
- **エディタの関数セレクタは、項目を押しても選べなかった。****開いたファイルの先頭の公開関数が選ばれるので、補助関数を `_` 付きの非公開にし、走らせたい関数を 1 ファイル 1 つにした**

**ここで見ていないもの。** **リンクで落ちない理由の切り分け**（**サンドボックスの `allow-downloads` と見ているだけである**）。**2 手目の印が落ちる間隔の切り分け**と、**範囲の貼り付けで印が付くか**。
**崩したシートで名指しして止まるか**（**崩す操作をしていない**）。

**確認に使ったスプレッドシートはまだ消していない。****消すのは人の手で行う**（→ 下の「確認に使うファイルの規約」）。

## コピーで何が渡ったか（項目 5）

**渡った。** 2026-09-21、**別のアカウント**（大学のアカウント）**でコピーして開き、メニューの 3 項目がどれも動いた。**
**6-1 の #3 は立っている。**配り方は変えない（→ 6 の #1）。**ADR は起こしていない。**

**なぞった順である。本番の配り方と同じ** — 原本を渡し、担当者が**コピーを 1 回**取る。

| # | やったこと | 見たもの |
| --- | --- | --- |
| 1 | 原本を別のアカウントに**閲覧者**で共有した | 開くと**閲覧のみ**になる。**この状態ではメニュー「シフト」が出ない**（`onOpen` が閲覧者では走らない）。**コピーを取れば出る**（→ 3） |
| 2 | **ファイル → コピーを作成** | ダイアログに「**添付されている Apps Script ファイル**」として**スクリプトのプロジェクト名が出る**。**「Apps Script ファイルと権限もコピーされます」**の注意書きも付く |
| 3 | コピーを開いた | **5 枚がそろい、生成シート 3 枚の保護も渡っていた**（タブに鍵が付く）。**メニュー「シフト」が出た。**項目も 3 つそろっている |
| 4 | シフト → **フォームを作る** を押した | 「**認証が必要です** — このドキュメントに添付されたスクリプトを実行するには、あなたの許可が必要です」。**OK で承認の窓が開く** |
| 5 | 承認を通してから、**3 項目を押した** | **3 つとも動いた。**`「フォームを作る」はまだ作っていない（issue #144）` ／ `「生成」はまだ作っていない（issue #151）` ／ `「画像を書き出す」はまだ作っていない（issue #157）` |
| 6 | **拡張機能 → Apps Script** を開いた | **4 ファイルとも渡っていた** — `appsscript.json` ／ `シート構成.gs` ／ `テンプレートを組み立てる.gs` ／ `メニュー.gs`。**日本語のファイル名もそのままである。**`appsscript.json` の中身は [`appsscript.json`](appsscript.json) と同じだった |

**5 つ、書いておく。**

| 見たもの | |
| --- | --- |
| **大学のアカウントだと、コピーを開いた直後に「セキュリティの制限」が 1 枚出る** | 「**ファイル オーナーの組織のポリシーに基づいて制限が適用されます**」「**共有は特定の受信者に限定されます**」。**コピーの持ち主が大学のアカウントになったために出ている。****止めているのは共有であって、スクリプトではない** — このあと 4〜6 が全部通っている |
| **担当者が最初に見るのは「認証が必要です」である** | **エディタの「承認が必要です」ではない**（→ [`docs/画面の記録/`](../docs/画面の記録/) の 0001）。**担当者はエディタを開かない**ので、入口の 1 枚がそもそも別である。**要求するスコープの判定は [`README.md`](README.md) の「要求するのは 3 スコープである」が持つ。ここは見たものを書いただけである** |
| **スクリプトのプロジェクトはコピーごとに別物になる** | スクリプト ID が原本と違う。**承認もコピーごとに 1 回ずつ要る** — 2 の「担当者がやることの全部」の操作 2（**最初に 1 回**）と同じで、増えていない |
| **`appsscript.json` をエディタに出す設定は渡らない** | [`README.md`](README.md) の手順 5 で入れた「**`appsscript.json` マニフェスト ファイルをエディタで表示する**」が、**コピー側では外れていた。****マニフェストそのものは渡っている**（タイムゾーンも V8 も効いている）。**担当者はマニフェストを見ないので、操作は増えない** |
| **原本のプロジェクト名が、担当者のコピーの画面に出る** | 2 のダイアログに出るのは**原本側で付けた名前**である。**`無題のプロジェクト` のままだと、担当者はその名前を見る**（→ [`README.md`](README.md) の手順 4） |

## セルの表現がどう返ってきたか（項目 8）

**手元の前提どおりだった。** 2026-09-21、**[`core.js`](core.js) と [`shell.js`](shell.js) を足したうえで**、
条件入力・回答に値を入れて `getValues()` の返りを見た（→ [#137](https://github.com/jun-eg/school-festival-shift/issues/137)）。
**[`shell.js`](shell.js) の `表現を揃える` は直していない。**

**日付も時刻も日時も、文字列ではなく `Date` で返る。**

| セルに入れたもの | `getValues()` が返した型 | 返った値（UTC 表示） | `getFullYear()` | `表現を揃える` の結果 |
| --- | --- | --- | --- | --- |
| `2025-11-01`（日付） | `[object Date]` | `2025-10-31T15:00:00.000Z` | **2025** | `2025-11-01` |
| `8:00`（時刻） | `[object Date]` | `1899-12-29T23:00:00.000Z` | **1899** | `08:00` |
| `20:00`（時刻） | `[object Date]` | `1899-12-30T11:00:00.000Z` | **1899** | `20:00` |
| `2025/09/23 16:31:09`（日時） | `[object Date]` | `2025-09-23T07:31:09.000Z` | 2025 | `2025-09-23 16:31:09` |
| `2`（数） | `[object Number]` | `2` | — | `2`（数のまま） |
| `  4年生  `（文字） | `[object String]` | `"  4年生  "` | — | `4年生`（両端だけ落ちる） |
| チェックボックス（オン） | `[object Boolean]` | `true` | — | `TRUE` |
| 空のセル | `[object String]` | `""` | — | `""` |

- **時刻だけのセルが `1899-12-30` を土台にした `Date` で返ることが、実機で立った。**
  **`shell.js` が年で日付と時刻を見分けているのは、この 1 行に乗っている**（`1899` と `2025`）
- **タイムゾーンは [`appsscript.json`](appsscript.json) の `Asia/Tokyo` が効いている。**
  UTC で `1899-12-29T23:00` が `08:00` に、`2025-09-23T07:31:09` が `16:31:09` になっている
- **`表現を揃える` が返したのは、`値の表現` に書いてある 3 つ（`YYYY-MM-DD` ／ `HH:MM` ／ `YYYY-MM-DD HH:MM:SS`）と数だけだった**

**ついでに分かったこと（コアと殻の側。手元の検査と同じ結果である）。**

| 何を見たか | 実機の結果 |
| --- | --- |
| **殻が読んだ入力が、そのままコアの入口を通るか** | **通った。**`表現を確かめる` が 1 件も止めていない |
| **入力の名前** | `日ごとの営業 4 時刻` ／ `役割と必要人数` ／ `調理責任者の学年` ／ `委員会の指定枠` ／ `準備・片付けのルール` ／ `回答` ／ `割り当て`。**コアの `入力の名前()` と一致した** |
| **区画ごとに切って読めるか** | **読めた。**見出しの 2 行を飛ばし、`役割と必要人数` は G 列から 5 列。**行の無い `委員会の指定枠` は空の配列**で、**隣の区画の空行を拾っていない** |
| **骨組みのまま走らせたとき** | 未了が 6 つとも名指しで返った（`取り込む #146` ／ `展開する #149` ／ `生成する #151` ／ `違反を数える #141` ／ `未充足を名指しする #142` ／ `指標を出す #154`）。**割り当てシートに置いた手直しは残っていた**（→ 5-3） |
| **段を全部そろえたとき** | 未了 0。割り当て・検証結果（違反 1 行 ＋ 未充足 1 行）・指標が書かれた |
| **列がずれた行を返したとき** | **止まった** —「`「指標」に返された 1 行目の列数が構成と違う。いま: 2 ／ 構成: 5（学籍番号 / 氏名 / 合計時間 / シフト回数 / 準備回数）`」 |
| **`.gs` が 4 つから 6 つになっても組み立てが通るか** | **通った。**`テンプレートを組み立てる` を走らせて `5 枚のうち保護したのは 3 枚である`。**「作った」も「見出しを置いた」も出ない**（2 回目以降と同じ） |

**この表は、列をずらす前の版で見たものである** — **`日ごとの営業時刻` はまだ `日ごとの営業 4 時刻` で、区画は 1 列ずつ左にあった**
（→ [ADR tech-requirements-0006](../docs/ADR/tech-requirements/0006-営業4時刻からは日の終わりが決まらず片付け終了が増えた.md)）。
**見たものは書き換えない。****いまの `src/` で走らせた回は別にある**
（→ 下の「[数える側を入れてから走らせた](#数える側を入れてから走らせた185)」。
**そこでは列をずらした後の 5 区画が、同じ見分け方のまま型に乗っている**）。

**貼った順は `シート構成` → `テンプレートを組み立てる` → `メニュー` → `コア` → `殻` である。**
**`コア.gs` と `殻.gs` が後ろでも通った** — どちらも [`sheet-layout.js`](sheet-layout.js) を関数の中でしか見ていないためである。
**逆の順は試していない。**

## 保護がどう出たか（項目 6）

**生成シートのセルを書き換えようとすると、確認が 1 枚出る。**

> **実行してもよろしいですか？**
> シート上の保護された範囲を編集しようとしています。編集してもよろしいですか？
> ☐ **5 分間このメッセージを表示しない**
> 〔キャンセル〕〔OK〕

**2 つ、想定より弱かった。判断は動かないが、書いておく。**

| 見たもの | 何が違ったか |
| --- | --- |
| **「5 分間このメッセージを表示しない」のチェックボックスがある** | [`README.md`](README.md) は「**押し切れば書ける**」と書いている。**実際は 1 回チェックすると、5 分間は確認すら出ない。**予防としてはさらに弱い |
| **保護の説明がこの画面に出ない** | [`sheet-layout.js`](sheet-layout.js) の `保護の説明`（`スクリプトが書くシートである（手で書き換えない）`）は、**この確認の画面には出なかった。**担当者の目に入るのは Google の既定の文だけである |

**「警告のみ」が採れるいちばん強い形であること**（**持ち主は締め出せない**）**は動いていない**ので、ADR は起こしていない。
**もう片方の予防（走る前に構造を検証し、崩れていれば名指しで出す）に寄る理由が 1 つ増えた**
（→ [`README.md`](README.md) の「走る前に構造を検証する」・[#138](https://github.com/jun-eg/school-festival-shift/issues/138)・2 の「止まる箇所」#8）。

## 初回承認で出た画面

**画像は [`docs/画面の記録/`](../docs/画面の記録/) にある**（規約は下の「確認に使うファイルの規約」）。
**3 組ある。**スコープを手で書く前（4 枚）・`spreadsheets.currentonly` 1 つだけのとき（2 枚）・**いまの 3 つのとき（4 枚）**である。
**手順書に貼るのはいちばん後の組である**（**いまのマニフェストで出るのはそれだけである**）。
**手順書のどこに貼るかは [#159](https://github.com/jun-eg/school-festival-shift/issues/159) が決める。**

### `oauthScopes` を書く前（2026-09-20）

**手順書に貼るのはこれではない。****撮った時点で [`appsscript.json`](appsscript.json) に `oauthScopes` を 1 つも書いていない。**
**自動判定が広いほうを 1 つ割り当てた状態の画面である**（下の「0004 に出た権限」）。
**いまのマニフェストは 3 つ手で書いてある**（`spreadsheets.currentonly` ／ `forms` ／ `script.container.ui`。
→ [`README.md`](README.md) の「要求するのは 3 スコープである」）ので、
**この 4 枚は、いまテンプレートを作ったときに出る画面と一致しない。**
**消さない** — **見たものを後から書き換えない。**

| 連番 | 何の画面か | どこで押すか | 画像 |
| --- | --- | --- | --- |
| 0001 | **「承認が必要です」** | Apps Script のエディタ。`権限を確認` | [`0001-承認が必要です.png`](../docs/画面の記録/0001-承認が必要です.png) |
| 0002 | **「このアプリは Google で確認されていません」** | 別ウィンドウ。`詳細` | [`0002-このアプリは確認されていません.png`](../docs/画面の記録/0002-このアプリは確認されていません.png) |
| 0003 | **同・詳細を表示した状態** | `（安全ではないページ）に移動` **がここで初めて出る** | [`0003-このアプリは確認されていません-詳細を表示.png`](../docs/画面の記録/0003-このアプリは確認されていません-詳細を表示.png) |
| 0004 | **「アカウントへのアクセスを求めています」** | 要求する権限の一覧。`続行` | [`0004-アカウントへのアクセスを求めています.png`](../docs/画面の記録/0004-アカウントへのアクセスを求めています.png) |

**3 つ、書いておく。**

| 見たもの | |
| --- | --- |
| **アカウントの選択は出なかった** | **ブラウザに 1 アカウントしかログインしていない状態で通した。**複数ログインしていれば 0001 と 0002 の間に 1 枚増える。**担当者の環境でどちらになるかは分からない**（→ [#160](https://github.com/jun-eg/school-festival-shift/issues/160)） |
| **0004 に出た権限は 1 つだけだった** | `Google スプレッドシートのすべてのスプレッドシートの参照、編集、作成、削除`（＝ `.../auth/spreadsheets`）。**`oauthScopes` を 1 つも書いていない状態で、自動判定が割り当てた 1 つである。****`script.container.ui` は出ていない** — **メニューを 1 つ出すだけなら要らない**ことが、この 1 枚で立っている（→ [`README.md`](README.md) の「書かないもの」） |
| **プロジェクト名が「無題のプロジェクト」のまま写っている** | 0001・0003・0004 の 3 枚。[`README.md`](README.md) の手順 4（**プロジェクトに名前を付ける**）を踏む前の画面である。**名前を付ければ、そこがその名前になる** |

### `oauthScopes` を書いた後（項目 9）

**手順書に貼るのはこちらである。2026-09-21 に撮った。**
**[`appsscript.json`](appsscript.json) に `spreadsheets.currentonly` を 1 つ書いた状態で、新しく作ったスプレッドシートに
[`README.md`](README.md) の「テンプレートの作り方」を 1 〜 9 までなぞった。**
**プロジェクト名は `学祭シフト` を付けてある**（手順 4 を踏んだ後の画面である）。

**2 枚だった。前の組の 4 枚から 2 枚減っている。**

| 連番 | 何の画面か | どこで押すか | 画像 |
| --- | --- | --- | --- |
| 0005 | **「承認が必要です」** | Apps Script のエディタ。`権限を確認` | [`0005-承認が必要です-スコープを書いた後.png`](../docs/画面の記録/0005-承認が必要です-スコープを書いた後.png) |
| 0006 | **「学祭シフト が Google アカウントへのアクセスを求めています」** | 別ウィンドウ。要求する権限の一覧。`続行` | [`0006-アカウントへのアクセスを求めています-スコープを書いた後.png`](../docs/画面の記録/0006-アカウントへのアクセスを求めています-スコープを書いた後.png) |

**4 つ、書いておく。**

| 見たもの | |
| --- | --- |
| **「このアプリは Google で確認されていません」が出なかった** | **前の組の 0002・0003 に当たる 2 枚が、間に 1 枚も挟まらなかった。**`権限を確認` から 0006 へ直行した。**承認が残っていたのではない** — **プロジェクトはこの日に新しく作ったもので**（スクリプト ID が別）、**残っていれば 0005 も 0006 も出ない** |
| **ただし、原因は切り分けていない** | **Google は「未確認アプリの警告は、sensitive か restricted のスコープを使っていて検証を受けていないときに出る」と書いている**（[Apps Script の承認](https://developers.google.com/apps-script/guides/services/authorization)）ので、**狭めたから消えたというのがいちばん素直な説明である。**それでも **同意画面そのものが 0004 と別のデザインになっている**ので、**Google 側の作り替えという説を潰せていない。****広いほうへ戻して 1 周踏めば分かるが、踏んでいない** — **実地（[#160](https://github.com/jun-eg/school-festival-shift/issues/160)・M5）で 1 周見るからである。****判定はここが持たない** |
| **0006 に出た権限は 1 つだけだった** | `このアプリケーションがインストールされているスプレッドシートの表示と管理`。**前の組の 0004（`すべてのスプレッドシートの参照、編集、作成、削除`）から狭まっている。**要求したのは `spreadsheets.currentonly` 1 つである |
| **アカウントの選択は出なかった** | 前の組と同じ条件である（**ブラウザに 1 アカウントしかログインしていない**）。**担当者の環境でどちらになるかは分からない**（→ [#160](https://github.com/jun-eg/school-festival-shift/issues/160)） |

**1 スコープで足りた。** 承認を通した後、**`buildTemplate` が権限のエラーを 1 つも出さずに走り切った**
（`5 枚のうち保護したのは 3 枚である` まで。実行ログは前の回と同じ）。
**メニューも動いた** — スプレッドシートを開き直すと「シフト」が出て、`生成` を押すと
`「生成」はまだ作っていない（issue #151）` がトーストで出た。
**`SpreadsheetApp.getUi()`・`createMenu`・`addToUi`・`toast` が、`spreadsheets.currentonly` だけで通っている**
— **メニューを出すだけなら `script.container.ui` は要らない。**
**その後 [#144](https://github.com/jun-eg/school-festival-shift/issues/144) でダイアログ（名簿の画像を選ぶ）が入り、いまは要求している**
（→ [`README.md`](README.md) の「要求するのは 3 スコープである」）。

### `oauthScopes` が 3 つになった後（項目 13）

**いま手順書に貼るのはこれである。2026-09-22 に撮った**（→ [#199](https://github.com/jun-eg/school-festival-shift/issues/199)）。
**[`appsscript.json`](appsscript.json) の 3 つ**（`spreadsheets.currentonly` ／ `forms` ／ `script.container.ui`）**をそのまま貼った状態で、
新しく作ったスプレッドシートに [`README.md`](README.md) の「テンプレートの作り方」を 1 〜 8 までなぞった。**
**プロジェクト名は `シフト` を付けてある**（手順 4 を踏んだ後の画面である）。

**4 枚だった。1 スコープのときの 2 枚から 2 枚増え、書く前の 4 枚と同じ枚数に戻っている。**

| 連番 | 何の画面か | どこで押すか | 画像 |
| --- | --- | --- | --- |
| 0007 | **「承認が必要です」** | Apps Script のエディタ。`権限を確認` | [`0007-承認が必要です-3スコープ.png`](../docs/画面の記録/0007-承認が必要です-3スコープ.png) |
| 0008 | **「このアプリは Google で確認されていません」** | 別ウィンドウ。`詳細` | [`0008-このアプリはGoogleで確認されていません-3スコープ.png`](../docs/画面の記録/0008-このアプリはGoogleで確認されていません-3スコープ.png) |
| 0009 | **同・詳細を表示した状態** | `シフト（安全ではないページ）に移動` **がここで初めて出る** | [`0009-このアプリはGoogleで確認されていません-詳細を表示-3スコープ.png`](../docs/画面の記録/0009-このアプリはGoogleで確認されていません-詳細を表示-3スコープ.png) |
| 0010 | **「シフト が Google アカウントへのアクセスを求めています」** | 要求する権限の一覧。**チェックを入れてから** `続行` | [`0010-アカウントへのアクセスを求めています-3スコープ.png`](../docs/画面の記録/0010-アカウントへのアクセスを求めています-3スコープ.png) |

**4 つ、書いておく。**

| 見たもの | |
| --- | --- |
| **「このアプリは Google で確認されていません」が戻ってきた** | **1 スコープのときは出なかった 2 枚**（前の節の「4 つ、書いておく」の 1 つめ）**が、3 つにしたら出た。****前の節で潰せていなかった説のうち「狭めたから消えた」のほうが残る** — **スコープを増やしたら同じ画面が戻ってきたからである。**増えた 2 つのうち **`forms` が sensitive である**ことが、いちばん素直な説明になる。**それでも「どのスコープが効いたか」は 1 つずつ足して確かめてはいない**（**判定はここが持たない** → [#160](https://github.com/jun-eg/school-festival-shift/issues/160)） |
| **同意画面が「1 つずつ選ぶ」形になっていた** | 0010 には **`すべて選択` と、スコープごとのチェックボックスが 3 つ**並ぶ。**0004・0006 には無かった形である。****担当者はチェックを入れてから `続行` を押すことになる** — **操作は 2「担当者がやることの全部」の 2（最初に 1 回の権限承認）の中で、増えてはいない。****ただし手順書には「全部にチェックを入れる」と書く必要がある**（→ [#159](https://github.com/jun-eg/school-festival-shift/issues/159)）。**1 つでも外すと、外したスコープを使う所で落ちる** |
| **0010 に並んだ権限は 3 つで、マニフェストの 3 つと同じだった** | `このアプリケーションがインストールされているスプレッドシートの表示と管理`（`spreadsheets.currentonly`） ／ `Google ドライブのフォームの表示と管理`（`forms`） ／ `Google アプリケーション内のプロンプトとサイドバーで、サードパーティのウェブコンテンツを表示、実行します`（`script.container.ui`）。**広いほうの `すべてのスプレッドシート` は出ていない** |
| **アカウントの選択は出なかった** | 前の 2 組と同じ条件である（**ブラウザに 1 アカウントしかログインしていない**）。**担当者の環境でどちらになるかは分からない**（→ [#160](https://github.com/jun-eg/school-festival-shift/issues/160)） |

**承認の別ウィンドウを、この回は Claude が通した。**
**`権限を確認` が `window.open` に渡した URL を、同じタブの `location.href` に入れ直して開いた** —
**別ウィンドウはブラウザ操作の側から掴めない**ためである（**前の 2 組は人の手で通した**）。
**同じ URL をたどっただけで、押した順（`詳細` → `（安全ではないページ）に移動` → チェック → `続行`）は変えていない。**

## 数える側を入れてから走らせた（[#185](https://github.com/jun-eg/school-festival-shift/issues/185)）

**2026-09-21、いまの `src/` の `.js` 9 つを本物のスプレッドシートに貼って 1 回走らせた。**
**上の「[セルの表現がどう返ってきたか](#セルの表現がどう返ってきたか項目-8)」は列をずらす前の版で見たもので、
[`count-violations.js`](count-violations.js) も [`name-unmet.js`](name-unmet.js) もまだ 1 行も無かった。**
**ここはその後である。**

**見たのは 4 つで、どれも手元の前提どおりだった。外れた前提は無い。**

| # | 見たこと | 実機の結果 |
| --- | --- | --- |
| 1 | **未了の名指しが 6 つから 4 つに減るか** | **減った。**`取り込む #146` ／ `展開する #149` ／ `生成する #151` ／ `指標を出す #154` の 4 つで、**`違反を数える` と `未充足を名指しする` はそこに出ない** — `builtInSteps` と `coreSteps` は食い違っていない（→ [`core.js`](core.js)） |
| 2 | **1 つ貼り忘れると名指しして止まるか** | **止まった。**[`count-violations.js`](count-violations.js) ／ [`name-unmet.js`](name-unmet.js) の**どちらを外しても**名指しで落ち、**未了の行は 1 つも出ない**（混ざらない） |
| 3 | **検証結果シートに種別つきの行が書かれるか** | **書かれた。****「警告のみ」の保護がかかった 3 枚に、スクリプトは確認を 1 枚も出さずに書いた** |
| 4 | **列をずらした後の条件入力 5 区画が 5-1 の型に乗るか** | **乗った。**[`shell.js`](shell.js) の `normalizeValue` も [`input-types.js`](input-types.js) の `toType` も**直していない** |

**貼ったのは 9 つ ＋ [`appsscript.json`](appsscript.json) で、`.gs` の名前も中身もリポジトリのものである**
（**10 ファイルとも SHA-256 の先頭 16 桁を突き合わせて一致を見てから保存した**）。
**それとは別に、使い捨ての `.gs` を足してある** — `runOnActiveSpreadsheet` の**返り値は実行ログに出ない**ので、
**名指しを `Logger.log` に書き出す口**が要る。**リポジトリの `src/` には入れない。**

### 1 — 未了は 4 つで、数える側 2 つはそこに出ない

**`runOnActiveSpreadsheet()` を段を渡さずに走らせた。**

```
未了 4 件
未了: 取り込む（issue #146）→ null
未了: 展開する（issue #149）→ null
未了: 生成する（issue #151）→ 割り当て
未了: 指標を出す（issue #154）→ 指標
「割り当て」の行数（見出しを除く）: 0
「検証結果」の行数（見出しを除く）: 0
「指標」の行数（見出しを除く）: 0
```

**未了が 1 つでもあるので、生成シートは 1 枚も書かれていない**（→ [`shell.js`](shell.js) の `writeOutputs`）。
**下の 3 を先に走らせた後にもう 1 度これを走らせたが、そのとき書かれていた 58 行はそのまま残った** —
**空の配列で上書きしていない**（→ 5-3）。

### 2 — 貼り忘れは、未了に混ざらずに名指しで止まる

**[`count-violations.js`](count-violations.js) を消して走らせた。**

```
Error: count-violations.js が貼られていない（「違反を数える」の中身がそこにある → issue #141）
builtInSteps            @ core.gs:49
build                   @ core.gs:101
run                     @ shell.gs:132
runOnActiveSpreadsheet  @ shell.gs:117
```

**[`name-unmet.js`](name-unmet.js) のほうも同じだった**
（`Error: name-unmet.js が貼られていない（「未充足を名指しする」の中身がそこにある → issue #142）`）。
**どちらも `未了 … 件` の行より前で落ちている** — **貼り忘れが「まだ作っていない」に化けていない。**

**スタックトレースは英字の関数名で出た**（前は日本語だった → [#175](https://github.com/jun-eg/school-festival-shift/issues/175)／[#178](https://github.com/jun-eg/school-festival-shift/issues/178)）。

### 3 — 保護のかかった検証結果に、確認を出さずに書けた

**まだ中身の無い段 4 つを使い捨ての段（どれも空の配列を返すだけ）で埋めて、未了を 0 にした。**

```
未了 0 件
「割り当て」の行数（見出しを除く）: 0
「検証結果」の行数（見出しを除く）: 58
「指標」の行数（見出しを除く）: 0
種別ごと: {"未充足":58}
先頭: ["未充足","2025-11-01","08:00","08:30","調理","","","必要人数: 2 人に対して 0 人しか置いていない",2]
末尾: ["未充足","2025-11-02","20:30","21:00","調理","","","必要人数: 2 人に対して 0 人しか置いていない",2]
```

**確認は 1 枚も出なかった。**「警告のみ」の保護は**スクリプトの書き込みを止めない**
（担当者が手で書き換えるときだけ出る → 上の「[保護がどう出たか](#保護がどう出たか項目-6)」）。**保護のかけ方は直していない。**

**生成（[#151](https://github.com/jun-eg/school-festival-shift/issues/151)）が無いあいだに何が出るかも、そのまま書く。**
**割り当ては 0 行で、違反は 0 件、未充足は需要が全部名指しされる** — **これが「数える側だけを先に入れて、生成が無いまま回せる」の実機側である**
（→ [`core.js`](core.js) の `build`）。**未充足が大量に出ること自体は外れではない。**

**何行になるかはここが判定しない。** **行数を決めるのは入れた条件入力の中身**であって、
**数え方そのものは手元の検査が持つ**（→ 上の「[手元で分かる範囲と、実機でしか分からない範囲](#手元で分かる範囲と実機でしか分からない範囲)」）。
**[`count-violations.js`](count-violations.js) も [`name-unmet.js`](name-unmet.js) も `SpreadsheetApp` を 1 度も掴まない**ので、
**実機と手元で結果が変わる余地が無い。ここで数え直さない。**

**検証結果に書いた値は、Google スプレッドシートが日付・時刻として読み直す** —
`getValues()` で読み戻すと `2025-11-01` は `2025-10-31T15:00:00.000Z` の `Date` に、`08:00` は `1899-12-29T23:00:00.000Z` になっていた。
**画面の表示は書いた文字列のままである。**生成シートは**コアが読まない**（→ [`core.js`](core.js) の `sheetsNotRead`）ので、この読み直しはどこにも戻ってこない。

### 4 — 実機のセルが、5-1 の型に乗った

**条件入力の 5 区画に値を入れ、`readInputs` が返した行と、そこから `takeConditions` が作った型を見た。**

```
日ごとの営業時刻: [["2025-11-01","08:00","10:00","18:00","18:00","21:00"],["2025-11-02","08:00","10:00","18:00","18:00","21:00"]]
役割と必要人数: [["","","","調理",2],["2025-11-01","10:00","12:00","接客",3]]
調理責任者の学年: [["3年生"],["4年生"]]
委員会の指定枠: [["2025-11-02","16:10","17:00","クリーンパトロール",3]]
準備・片付けのルール: [["午前と午後の境目","12:00"]]

枠: 2025-11-01 は 26 枠 / 2025-11-02 は 26 枠
役割と必要人数: [{"date":"","start":"","end":"","role":"調理","count":2},{"date":"2025-11-01","start":"10:00","end":"12:00","role":"接客","count":3}]
調理責任者の学年: ["3年生","4年生"]
委員会の指定枠: [{"date":"2025-11-02","start":"16:10","end":"17:00","role":"クリーンパトロール","count":3}]
準備・片付けのルール: {"noonBoundary":"12:00"}
```

- **列をずらした後の位置（A〜F ／ H〜L ／ N ／ P〜T ／ V〜W）から、区画ごとに切って読めている**
- **日付のセルは `YYYY-MM-DD`、時刻のセルは `HH:MM`、人数は数のまま**で `normalizeValue` を通った
  （**上の「セルの表現がどう返ってきたか」の表と同じ**である。**見分け方は 1 行も直していない**）
- **空の欄（日と時間帯を空けた行）は空文字のまま型に乗った** — 「絞らない」が実機のセルからも立つ（→ 5-1 の #2）

## 正規表現とエラーメッセージがどう入ったか（項目 10）

**設定できた。** 2026-09-22、**本物の Google フォームを [`build-form.js`](build-form.js) に作らせ、4 設問とも正規表現とエラーメッセージの文言が入った。**
**[`docs/tech-requirements.md`](../docs/tech-requirements.md) 6-1 の #1 は立っている** — **フォームは「定義から作る」のままである**（→ 6 の #6）。
**「完成品の複製」への切り替えは要らない。M1 ② の数え直しも要らない。****判定は [#145](https://github.com/jun-eg/school-festival-shift/issues/145) が持つ。ここは見たものを書いただけである。**

**4-3 の表がそのまま出た。****句点の揺れ ◎ は、揃わずに 11月3日(学祭2日目) だけに付いた。**

| 設問 | 正規表現 | エラーメッセージ | 句点 |
| --- | --- | --- | --- |
| 11月1日(準備日) | **4-2 の 1 本** | `無効な書式です` | 無し |
| 11月2日(学祭1日目) | **同じ 1 本** | `無効な書式です` | 無し |
| **11月3日(学祭2日目)** | **同じ 1 本** | **`無効な書式です。`** | **付いた** ◎ |
| 11月4日(片付け) | **同じ 1 本** | `無効な書式です` | 無し |

**2 か所で読んだ。どちらも同じだった。**

| どこで | 何を見たか |
| --- | --- |
| **フォームの編集画面** | **値の入った「パターン」は 6 つ**（学籍番号 ／ 友達欄 ／ 希望時間 4 設問）**で、そのうち 4 つが 4-2 の同じ 1 本だった。****エラーの文言が入っているのは希望時間 4 設問だけで、学籍番号と友達欄は空のままだった**（→ 4-3。**記録に無い文言を発明していない**） |
| **入る側の画面** | **4 設問に `10-15` を入れると、設問ごとに赤で出た。****出た文言は上の表のとおりで、11月3日 だけ句点が付いている。**`23:59-23:59,0:00-0:30` は通った（**時は 0-23、分は 00-59** ／ **1 桁の時** ／ **カンマ区切り** → 4-2） |

**正規表現は [`form-definition.js`](form-definition.js) の値と 1 文字も違わない。** 編集画面から読んだ文字列を手元で突き合わせた（`wishTimePattern` と厳密一致。**同じ 1 本が [`docs/tech-requirements.md`](../docs/tech-requirements.md) 4-2 にも載っている**）。

**設問の題と説明文の営業時間も、4-1・4-2 の表のとおりに出た**（→ 仕様 #2）。
**条件入力の「日ごとの営業時刻」に前回の日付（2025-11-01〜04）と営業時刻を入れて作った結果である。**

```
設問の題と説明文の営業時間を、条件入力の「日ごとの営業時刻」4 行から組んだ
（11月1日(準備日) 8:00-21:00 / 11月2日(学祭1日目) 8:00-20:00 /
  11月3日(学祭2日目) 8:00-20:00 / 11月4日(片付け) 8:00-15:00）
設問を 9 つ、画像アイテムを 1 つ置いた
回答先をこのスプレッドシート自身に向けた（1bWCyMJ9gTgqNV65doDOIY-b-lBrhQx9AtuHqcs_xLyE）
シート「回答」に保護をかけた（警告のみ）
フォームが作ったシートを「回答」にした
```

**一緒に見えたもの（項目 11・12）。**

| | 見たもの |
| --- | --- |
| **項目 11** | **`setDestination` は [`appsscript.json`](appsscript.json) の 3 スコープで通った。****フォームが作った回答シートは 1 枚**で、**「回答」に置き換わり、保護（警告のみ）もかかり直した**（タブに鍵が付く）。**見出しは タイムスタンプ ＋ 設問の題 9 つの 10 列で、頭 6 列は構成の列名と一致し、後ろ 4 列は今年の題だった**（→ 4-1） |
| **項目 12** | **`Utilities.newBlob` → `setImage` は通った。****画像アイテム「調理名簿」に入り、入る側の画面に出た** |

**この回に見ていなかったもの。** **どちらも [#199](https://github.com/jun-eg/school-festival-shift/issues/199) の回（同じ 2026-09-22）で見た。**

| | この回になぜ見ていないか |
| --- | --- |
| **項目 12 の残り半分** | **ダイアログの `<input type="file">` を触るところ。****`showModalDialog` の中身は別オリジンの iframe で、ブラウザ操作の側から掴めなかった**（ダイアログは開き、「ファイルを選択」と「フォームを作る」が出るところまでは見た）。**代わりに、画面が呼ぶのと同じ入口 `createFormFromPicker({ base64, mimeType, fileName })` を、使い捨ての `check-145.gs` から呼んだ** — **`build-form.js` にも `form-picker.html` にも手を入れていない**（→ 下の「[ダイアログの入口から 1 周通した](#ダイアログの入口から-1-周通した199)」で、**ダイアログの中の `<input type="file">` まで戻して見た**） |
| **項目 13** | **初回承認は 3 スコープで通ったが、出た画面は見ていない** — **別ウィンドウは人の手で通したためである。****枚数も順も数えていないので、[`docs/画面の記録/`](../docs/画面の記録/) に足した画像は無い**（→ 上の「[`oauthScopes` が 3 つになった後](#oauthscopes-が-3-つになった後項目-13)」で 4 枚撮った） |

**手元と実機の突き合わせ方が、前の回と違う。**
**2026-09-21 の回は SHA-256 の先頭 16 桁で突き合わせた。****この回は行数だけである** — **貼った 12 ファイルとも、実機の最終行がリポジトリ側の行数 ＋ 1 であることを 1 つずつ見た。**
**クリップボードを読み返す手がこのセッションに無かった**ためで、**SHA に戻せるならそちらが強い。**

## ダイアログの入口から 1 周通した（[#199](https://github.com/jun-eg/school-festival-shift/issues/199)）

**2026-09-22。**[#148](https://github.com/jun-eg/school-festival-shift/issues/148) が偽の器の上で数えた
**「人の手が入った箇所 0」**を、**本物のスプレッドシートと本物の Google フォームの上に当てた回である。**

**数え方はここが持たない。** **段の切り方も合格の線も
[`scripts/M1②の宣言.json`](../scripts/M1②の宣言.json) が持つ**（→ [#148](https://github.com/jun-eg/school-festival-shift/issues/148)）。
**ここに書くのは、段ごとに「通った／止まった」と、実機でしか出なかった差だけである。**

### 段ごとに、通ったか

| 段 | 実機で見たもの | |
| --- | --- | --- |
| **1 メニューを押す** | メニュー「シフト」→「フォームを作る」で、**ダイアログが 1 枚開いた**（[`0011-名簿の画像を選ぶ.png`](../docs/画面の記録/0011-名簿の画像を選ぶ.png)）。**確認の画面も追加の入力も出ない** | **通った** |
| **2 名簿の画像を選ぶ** | **ダイアログの `<input type="file">` に載せた画像が、そのまま画像アイテム「調理名簿」に入り、入る側の画面に出た。**渡したのは画像 1 つだけで、**フォームの名前も回答先の ID も聞かれない** | **通った** |
| **3 フォームを作る** | **設問 9 つと画像アイテム 1 つが置かれた。**題の日付と説明文の営業時間は、**条件入力の「日ごとの営業時刻」4 行から出ている**（`11月1日(準備日) 8:00-21:00` ／ `11月2日(学祭1日目) 8:00-20:00` ／ `11月3日(学祭2日目) 8:00-20:00` ／ `11月4日(片付け) 8:00-15:00`） | **通った** |
| **4 回答先を向ける** | **`setDestination` がこのスプレッドシート自身に向いた**（`1kcoIGlAqMdDJAVRZ1HVv3Qs9OBy-u8PUVVKLJ_uDLko`）。**「回答」に `getFormUrl()` が付いた** | **通った** |
| **5 回答シートを構成に繋ぐ** | **シートは 5 枚のまま**（`条件入力 / 回答 / 割り当て / 検証結果 / 指標`）。**「回答」が構成の位置にあり、保護（警告のみ）もかかり直した。****見出しは 10 列で、[`data/前回の希望データ-モック.csv`](../data/前回の希望データ-モック.csv) の見出しと 1 列ずつ同じだった** — **並べ替える手が要らない** | **通った** |
| **6 回答が積まれる** | **入る側から実際に 1 件送った。**モックの 1 行ぶんの値である。**「回答」に積まれ、取り込みが読むシートと同じ 1 枚だった**（下の「送るのと貼るのは、置かれる場所が違う」） | **通った** |
| **7 構造を照らす** | **崩れ 0 箇所。**そのまま先へ進んだ | **通った** |
| **8 回答シートを読む** | **51 行 × 10 列で読めた**（貼った 50 行 ＋ 送った 1 件）。行も列も落ちていない | **通った** |
| **9 値の表現を揃える** | **送った 1 件のタイムスタンプは `Date` で来て、`YYYY-MM-DD HH:MM:SS` に揃った。****日時のセルは 51 個とも揃った** | **通った** |
| **10 1 人 1 件に畳んで型にする** | **51 行が 39 件になり、学籍番号の重複 0。**乗った項目は `answers / canCook / grade / studentId` だけである。**未了として名指しされたのは `生成する（issue #151）` と `指標を出す（issue #154）` の 2 つ** | **通った**（**1 回目はここが名指しして止まった** → 下の「実機でしか出なかった差」） |

**人の手が入った箇所 0 箇所。一覧に無い操作 0 件。**
**押したのはメニュー 1 回と、ダイアログで画像を選んで「フォームを作る」1 回だけで、どちらも 2 の一覧 3 にある。**
**確認を求める画面も、追加の入力を求める画面も、段 1〜10 のあいだに 1 枚も出ていない。**
**数えていない画面が 2 つある。****初回承認の 4 枚は段 1 の手前**で、**2 の一覧の操作 2（最初に 1 回）である**
（→ 上の「[`oauthScopes` が 3 つになった後](#oauthscopes-が-3-つになった後項目-13)」）。
**保護の「実行してもよろしいですか？」は、判定の入力を手で貼った実装者の側に出たもので、担当者の経路には無い**（→ 下）。

**手元の判定と同じ数になった。** **39 件 ／ 重複 0 ／ 乗った項目 4 つ**は、
`node scripts/M1②の判定.mjs` が偽の器の上で出す値と 1 つずつ同じである。**ここで数え直してはいない。**

### 実機でしか出なかった差 — **貼った 50 行の 1 行目だけ、タイムスタンプが文字列で来た**

**1 回目は段 10 で止まった**（**段 9 は素通しだった**）。名指しはこう出た。

```
Error: 「回答」の 1 行目（シートの 2 行目）の「タイムスタンプ」が YYYY-MM-DD HH:MM:SS でない。
いま: 2025/09/23 16:31:09。畳み込みのキーなので、無いまま畳まない（→ 3 の規則 2 の ②）
at readTimestamp(take-in:111:11) … at takeIn(take-in:65:3)
at callStep(core:113:30) at build(core:120:18) at run(shell:135:18)
```

**止めたのは [`take-in.js`](take-in.js) の `readTimestamp` である** — **段 9（[`shell.js`](shell.js) の `normalizeValue`）は素通しだった。**
**文字列は両端の空白を落とすだけ**なので、`2025/09/23 16:31:09` がそのまま下りている。

**何が起きていたか。** **`回答` シートに 50 行を貼ったとき、1 行目のセルだけが文字列のままで、残り 49 行は日時になった。**
**`ISTEXT` で 50 個数えて `T` が 1 つ、`D` が 49 つ**である。**1 行目だけである。**

**これは実装の側の差ではない。**

| | |
| --- | --- |
| **送った行は `Date` で来た** | **入る側から送った 1 件**は、**Google が日時のセルとして書いた**。**段 9 も段 10 も止まらない。****本番でここを通るのは送られた行だけ**なので、**経路そのものは通っている**（→ 宣言の「数えないもの」の 3 つめ） |
| **止まったのは判定の入力の置き方である** | [`scripts/M1②の宣言.json`](../scripts/M1②の宣言.json) の「回答をどう置くか」は **「タイムスタンプは日時のセルとして置く（回答シートに貼れば日時になる）」**と書いている。**実機では、貼っても 1 行目だけ日時にならなかった。****前提が 1 つ外れている** |
| **どの分かれ道か** | **宣言の分かれ道の 1 つめ（判定の置き方を直す）である。****`src/` にも定義にも触らずに消えた** — **その 1 セルを消して同じ値を打ち直しただけで、51 個とも `D` になり、段 10 まで通った** |

**直す先はこの文書ではない。** **[`scripts/M1②の宣言.json`](../scripts/M1②の宣言.json) の「回答をどう置くか」を
実機に合わせるかどうかは [#203](https://github.com/jun-eg/school-festival-shift/issues/203) が決める。**
**この issue は数え方を動かさない**ので、**見たものを書いただけである。**

**なぜ 1 行目だけかは切り分けていない。** **フォームが作った回答シートの 1 行目（次の回答が入る行）に、
貼り付けが日時として解釈されない書式が付いていた**というのがいちばん素直な説明だが、
**書式を 1 つずつ見て確かめてはいない。**

### 送るのと貼るのは、置かれる場所が違う

**送った 1 件は、貼った 50 行の下ではなく上に入った。**
**Google が「回答」の 2 行目に行を挿入し、貼ってあった 50 行は 3〜52 行目へ下がった。**
**貼った行の下に足されるのではない。**
**なぜその位置かは切り分けていない**（**フォームが自分の知っている回答の数を持っていて、その次に挿入した**というのが素直な説明だが、
**2 件目を送って確かめてはいない**）。

**取り込みには効かない。** **[`take-in.js`](take-in.js) が採るのは「後から来た行」で、見ているのはタイムスタンプである**
（→ [`README.md`](README.md) の「取り込む側は、後から来た行を採る」）。**行の並びではない。**
**送った 1 件は、貼った 1 行目と同じ学籍番号にしてある**（モックの 1 行をそのまま送った）ので、
**51 行が 39 件に畳まれた** — **実人数は 39 人のままである。**

### どうやってダイアログの中を走らせたか（項目 12 の残り半分）

**`showModalDialog` の中身は別オリジンの iframe で、ブラウザ操作の側から `<input type="file">` を掴めない**
（**2026-09-22 の前の回で踏んだ壁と同じである**）。**`userCodeAppPanel` を単独のタブで開いても、中身は空で出る。**

**使い捨ての `check-199.gs` を 1 つ足して、こうした。**

```
HtmlService.createHtmlOutputFromFile('form-picker').getContent() で中身を読み、
その後ろに「ファイルを選ぶところを代わりにやる」script を 1 つ足して showModalDialog に渡す。
足した script がやるのは、DataTransfer で作った File を <input type="file"> に載せて、
画面のボタンを押すところまでである。
```

**[`form-picker.html`](form-picker.html) には 1 文字も手を入れていない。**
**FileReader が読むところ・`google.script.run` が呼ぶところ・`createFormFromPicker` が受けるところは、
本物のダイアログの中の本物の画面が走った。**
**ダイアログには選んだファイル名（`調理名簿.png`）が出て、「フォームを作っている…」に変わり、
できたフォームの記録が返ってきた。**

**踏んでいないのは、OS のファイル選択ダイアログ 1 枚だけである**（**Chrome の UI であって、この経路のコードではない**）。
**担当者がそこで画像を選べるかは実地で見る**（→ [#160](https://github.com/jun-eg/school-festival-shift/issues/160)）。

**実機の `appsscript.json` だけ、リポジトリのものと 1 か所違う状態で終わっている。**
**使い捨ての 3 つ**（`check199Picker` ／ `check199Path` ／ `check199Answers`）を
**拡張機能 → マクロ → マクロをインポートで登録した**ので、**マニフェストに `sheets.macros` が増えた。**
**エディタから走らせると `SpreadsheetApp.getUi()` が使えず**（`Cannot call SpreadsheetApp.getUi() from this context.`）、
**ダイアログを開く口がそこしか無かったためである。**
**`src/` の 12 ファイルと [`form-picker.html`](form-picker.html) は 1 文字も変えていない**（貼った時点で 13 ファイルとも SHA が一致している）。
**このスプレッドシートは残さない約束のもの**（→ 下の「確認に使うファイルの規約」）**なので、マニフェストは戻していない。**

### 保護の「警告のみ」は、貼る側に出た

**判定の入力（モック 50 行）を「回答」に貼ったとき、
「実行してもよろしいですか？ シート上の保護された範囲を編集しようとしています。」が出て、`OK` で貼れた。**
**持ち主は締め出されない**（→ [`README.md`](README.md)「保護は『警告のみ』である」）。

**担当者の操作は増えていない。** **この画面が出たのは、判定の入力を手で貼った実装者の側である。**
**入る側が送った 1 件では出ていない** — **フォームが書くのは担当者の編集ではないからである。**

## 走らせた記録

**1 回走らせるたびに 1 行足す。通らなかった回も消さない。**
**結果だけを書く。判断を新しく書かない** — 判断が動くなら [`docs/tech-requirements.md`](../docs/tech-requirements.md) に戻して ADR を 1 本起こす。

| 日付 | 走らせた人 | 載せ方 | 見た項目 | 結果 |
| --- | --- | --- | --- | --- |
| 2026-09-20 | jun-eg（権限承認のみ手動） | **手貼り**（`.gs` 3 つ ＋ `appsscript.json`） | **1・2・3・4・6・7** | **全部、手元と同じか想定どおり。**5 は別のアカウントが要るのでなぞっていない。**手順の穴が 2 つ見つかった**（→ [`README.md`](README.md) の手順 1・4） |
| 2026-09-20 | jun-eg | **同じスプレッドシート。承認を取り消して、初回承認をもう一度通した** | **初回承認の画面**（撮るため） | **4 枚撮れた**（→ 上の「初回承認で出た画面」）。**アカウントの選択は出なかった。**組み立ての結果は 1 回目と同じで、新しく分かったことは無い |
| 2026-09-21 | jun-eg（権限承認のみ手動）。**操作は Claude が Chrome で** | **原本を別のアカウントに閲覧者で共有 → そちらで コピーを作成** | **5** | **渡った**（→ 上の「コピーで何が渡ったか」）。**メニューの 3 項目とも動き、スクリプトは 4 ファイルとも入っていた。**コピー先は**大学のアカウント**で、**未確認アプリの承認は止められなかった**（「このアプリは確認されていません → 詳細 → 安全ではないページに移動」は出て、通った）。**6-1 の #5 に実機の観測が 1 つ付いた。判定はここが持たない** |
| 2026-09-21 | **Claude が Chrome で**（承認は済んでいたので手動は無し） | **同じスプレッドシートに [`core.js`](core.js) と [`shell.js`](shell.js) を足した**（エディタに直接。**貼るのはクリップボード経由では通らなかった**ので、エディタの中身を差し替えた）。**使い捨ての `確認.gs` も足した** | **8** ＋ **1〜4 の再確認** | **項目 8 は手元の前提どおり**（→ 上の「セルの表現がどう返ってきたか」）。**`表現を揃える` は直していない。**`.gs` が 6 つになっても `テンプレートを組み立てる` は通り、`5 枚のうち保護したのは 3 枚である`。**外れた前提は無い** |
| 2026-09-21 | jun-eg（**権限承認の別ウィンドウだけ手動**）。**それ以外は Claude が Chrome で** | **新しいスプレッドシートを 1 つ作り、[`README.md`](README.md) の「テンプレートの作り方」を 1 〜 9 までなぞった**（`.js` 6 つ ＋ `oauthScopes` を書いた [`appsscript.json`](appsscript.json)）。**エディタへは Monaco の `setValue` で流し込み、7 ファイルとも SHA-256 の先頭 16 桁をリポジトリ側と突き合わせて一致を見てから保存した** | **9** ＋ **1・2 の再確認** | **1 スコープで足りた。初回承認は 2 枚で、「このアプリは確認されていません」が出なかった**（→ 上の「スコープを書いた後に出た画面」）。**組み立ても、メニューの `getUi` と `toast` も通った。****切り分けの 1 周（広いほうに戻して出るか）は踏んでいない** |
| 2026-09-21 | jun-eg（**権限承認の別ウィンドウだけ手動**）。**それ以外は Claude が Chrome で** | **また新しいスプレッドシートを 1 つ作り、[`README.md`](README.md) の「テンプレートの作り方」を 1 〜 8 までなぞった**（**いまの `.js` 9 つ ＋ [`appsscript.json`](appsscript.json)**）。**エディタへは Windows のクリップボード経由で貼り**（`Monaco` の `setValue` は使っていない）、**10 ファイルとも SHA-256 の先頭 16 桁を突き合わせて一致を見てから保存した。**条件入力の 5 区画には**モックも実データも置かず、その場で組んだ 2 日ぶんの条件を貼った** | **[#185](https://github.com/jun-eg/school-festival-shift/issues/185) の 4 つ**（未了の数 ／ 貼り忘れ ／ 検証結果への書き込み ／ 条件入力が型に乗るか）＋ **1・2 の再確認** | **4 つとも手元の前提どおり。外れた前提は無い**（→ 上の「[数える側を入れてから走らせた](#数える側を入れてから走らせた185)」）。**`.gs` が 9 つになっても `buildTemplate` は通り、`5 枚のうち保護したのは 3 枚である`。**メニュー「シフト」も出た。**貼ったのは `count-violations.gs` → `name-unmet.gs` の順だが、貼る順に依存しないことは確かめていない**（消して戻す 2 回だけである） |
| 2026-09-22 | jun-eg（**権限承認の別ウィンドウだけ手動**）。**それ以外は Claude が Chrome で** | **また新しいスプレッドシートを 1 つ作り、[`README.md`](README.md) の「テンプレートの作り方」を 1 〜 9 までなぞった**（**いまの `.js` 11 つ ＋ [`form-picker.html`](form-picker.html) ＋ [`appsscript.json`](appsscript.json)**）。**エディタへは Windows のクリップボード経由で貼り**、**12 ファイルとも行数を突き合わせてから保存した**（**SHA-256 では突き合わせていない** → 上の節の最後）。**条件入力は「日ごとの営業時刻」4 行だけを埋めた**（**前回の日付 2025-11-01〜04 と営業時刻**。ほかの 4 区画は空である）。**使い捨ての `check-145.gs` も足した** | **10・11・12**（12 は半分）＋ **1・2 の再確認** | **正規表現もエラーメッセージも 4 設問とも設定できた。句点の揺れ ◎ も出た**（→ 上の「[正規表現とエラーメッセージがどう入ったか](#正規表現とエラーメッセージがどう入ったか項目-10)」）。**`setDestination` は 3 スコープで通り、回答シートは 1 枚で見出しは 10 列とも一致した。****外れた前提は無い。****13（承認の画面）は見ていない** |

| 2026-09-22 | **Claude が Chrome で**（**承認の別ウィンドウも Claude が通した** → 上の「`oauthScopes` が 3 つになった後」の最後） | **また新しいスプレッドシートを 1 つ作り、[`README.md`](README.md) の「テンプレートの作り方」を 1 〜 9 までなぞった**（**いまの `.js` 12 つ ＋ [`form-picker.html`](form-picker.html) ＋ [`appsscript.json`](appsscript.json)**）。**エディタへは Windows のクリップボード経由で貼り**、**14 ファイルとも SHA-256 の先頭 16 桁をリポジトリ側と突き合わせて一致を見てから保存した**（**行数ではなく SHA に戻した** → 上の節の最後）。**条件入力は「日ごとの営業時刻」4 行だけを埋めた**（**前回の日付 2025-11-01〜04**）。**使い捨ての `check-199.gs` も足した** | **[#199](https://github.com/jun-eg/school-festival-shift/issues/199) の 5 つ**（ダイアログの `<input type="file">` ／ 3 スコープの承認画面 ／ 貼った行が型 #6 になるか ／ 送った行が同じ経路を通るか ／ 一覧に無い操作が出ないか）＋ **項目 12・13** | **段 1〜10 が通り、人の手が入った箇所 0・一覧に無い操作 0。****39 件 ／ 重複 0 で、手元の判定と同じ数になった**（→ 上の「[ダイアログの入口から 1 周通した](#ダイアログの入口から-1-周通した199)」）。**外れた前提が 1 つ**: **貼った 50 行の 1 行目だけタイムスタンプが文字列で来て、段 10 が名指しして止まった。****宣言の分かれ道の 1 つめ（判定の置き方）で、`src/` は触っていない** |

| 2026-09-22 | **Claude が Chrome で**（**承認は済んでいたので手動は無し**） | **[#199](https://github.com/jun-eg/school-festival-shift/issues/199) と項目 14 で使ったのと同じスプレッドシート。貼り直していない**（**項目 14 の回から [`../src/`](.) は動いていない**）。**貼ってある 14 ファイル ＋ [`form-picker.html`](form-picker.html) の SHA-256 の先頭 16 桁をリポジトリ側と突き合わせ、1 つ残らず一致を見てから走らせた。****使い捨ての `check-153.gs` を 1 つ足しただけである**（**入力を読む手と、数えて突き合わせる手と、時計しか持たない。規則も数え方も 1 つも書いていない** — **実機に貼ってある `countViolations` ／ `nameUnmet` ／ `requiredAt` ／ `readAssignments` をそのまま呼ぶ**） | **項目 15**（[#153](https://github.com/jun-eg/school-festival-shift/issues/153) の M2） | **手元と同じだった。違反 0 件・名指しされていない未充足 0 件で合格**（→ 上の「[M2 を実機で判定したときの件数](#m2-を実機で判定したときの件数項目-15)」）。**未充足 83 件・あと 115 人、突き合わせ 704 ＝ 589 ＋ 115、超過 0 枠も手元と同じ。****案は 660 行とも同じで、SHA-256 の先頭 16 桁が一致した**（`032bb753cfd9519d`。→ 6 の #3 の理由 ③）。**コアは 0.443 ／ 0.587 秒、読みは 2.043 ／ 2.061 秒。****外れた前提は無い** |
| 2026-09-22 | **Claude が Chrome で**（**承認は済んでいたので手動は無し**） | **[#199](https://github.com/jun-eg/school-festival-shift/issues/199) と項目 14・15 で使ったのと同じスプレッドシート。貼り直していない**（**項目 14 の回から [`../src/`](.) は動いていない**）。**貼ってある 15 ファイルの SHA-256 の先頭 16 桁をリポジトリ側と突き合わせ、1 つ残らず一致を見てから走らせた**（**[`appsscript.json`](appsscript.json) だけは `sheets.macros` の 3 つが増えているぶん差が出る。3 スコープも `timeZone` も `runtimeVersion` も同じである**）。**条件入力の「日ごとの営業時刻」の真ん中 3 つ（`調理開始`・`調理終了`・`片付け開始`）を、確定シフトから算出した値に入れ替えた**（**`準備開始` と `片付け終了` は動かしていない** → [#209](https://github.com/jun-eg/school-festival-shift/issues/209)）。**使い捨ての `check-209.gs` を 1 つ足しただけである**（**帯の定義も役割名も 1 つも書いていない** — **実機に貼ってある `prepCleanupBands` ／ `isInBand` ／ `ruleRoles` をそのまま呼ぶ**）。**M2 の 2 つの線を数えるのは、前回と同じ `check-153.gs` の `checkM2` で、1 文字も直していない** | **項目 15**（**5 時刻が動いた後の M2** → [#209](https://github.com/jun-eg/school-festival-shift/issues/209)） | **手元と同じだった。違反 0 件・名指しされていない未充足 0 件で合格**（→ 上の「[5 時刻が動いた後に判定し直した](#5-時刻が動いた後に判定し直した209)」）。**未充足 59 件・あと 73 人、突き合わせ 704 ＝ 631 ＋ 73、超過 0 枠も手元と同じ。****案は 681 行とも同じで、SHA-256 の先頭 16 桁が一致した**（`3170caaab9ef923c`。→ 6 の #3 の理由 ③）。**帯が立ったので `準備` が 24 件出た**（**前の回は 0 件**）。**`片付け` は 26 件。****コアは 0.387 秒、読みは 2.041 秒。****外れた前提は無い** |
| 2026-09-22 | **Claude が Chrome で**（**承認は済んでいたので手動は無し**） | **[#199](https://github.com/jun-eg/school-festival-shift/issues/199) と項目 14・15 で使ったのと同じスプレッドシート。**今回は [`../src/`](.) が動いているので、4 ファイルを貼り直した**（[`sheet-layout.js`](sheet-layout.js) ／ [`input-types.js`](input-types.js) ／ [`name-unmet.js`](name-unmet.js) ／ [`generate.js`](generate.js) → [#210](https://github.com/jun-eg/school-festival-shift/issues/210)）。**貼る前に 15 ファイルが `main` と一致することを、貼った後に 15 ファイルが作業ブランチと一致することを、どちらも SHA-256 の先頭 16 桁で確かめた**（**エディタを開き直してから測り直している** — **保存がサーバへ載ったかを見るため**。**[`appsscript.json`](appsscript.json) だけは `sheets.macros` の 3 つが増えているぶん差が出る**）。**エディタへは Windows のクリップボード経由で貼った。****条件入力には「委員会の指定枠」の 2 行だけを足した**（`2025-11-02 16:00-17:00` ／ `2025-11-03 12:00-12:40` の `クリーンパトロール` 各 3 人。**ほかの区画は 1 セルも動かしていない**）。**M2 の 2 つの線を数えるのは前回と同じ `check-153.gs` の `checkM2` だが、今回は 1 行だけ直している** — **`requiredAt` に日付の文字列ではなく日そのものを渡す 1 行で、手元の [`../scripts/M2の判定.mjs`](../scripts/M2の判定.mjs) で直したのと同じ箇所である** | **項目 15**（**需要に時間帯が入った後の M2** → [#210](https://github.com/jun-eg/school-festival-shift/issues/210)） | **手元と同じだった。違反 0 件・名指しされていない未充足 0 件で合格**（→ 上の「[需要に時間帯が入った後に判定し直した](#需要に時間帯が入った後に判定し直した210)」）。**未充足 2 件・あと 2 人、突き合わせ 268 ＝ 266 ＋ 2、超過 0 枠も手元と同じ。****案は 300 行とも同じで、SHA-256 の先頭 16 桁が一致した**（`badd39820c91bf47`。→ 6 の #3 の理由 ③）。**需要のべが実機でも 268 だった**（**「全枠に効く」のままなら 704 である**）。**`クリーンパトロール` が 12 人分立ち、規則 6 がこの経路で初めて試された。****コアは 0.403 秒、読みは 3.190 秒。****外れた前提は無い** |
| 2026-09-22 | **Claude が Chrome で**（**承認は済んでいたので手動は無し**） | **[#210](https://github.com/jun-eg/school-festival-shift/issues/210) の判定に続けて、同じスプレッドシートで****生成シート 3 枚に書き戻した。****[`../src/`](.) は 1 文字も動かしていない**（貼り直してもいない）。**使い捨ての `check-210.gs` を 1 つ足しただけである** — **規則も数え方も書き戻し方も 1 つも持たず、実機に貼ってある [`shell.js`](shell.js) の `run` をそのまま呼ぶ。****`指標を出す` が未了で殻は本来 1 枚も書かない**（→ `writeOutputs`）**ので、段を差し替える口（[`core.js`](core.js) の `build` の `steps`）に空の指標を返す段を 1 つ渡している** | **書き戻し**（**項目 15 の「ここで見ていないもの」に入っていたもの**） | **3 枚とも入った。****割り当て 300 行 ／ 検証結果 2 行（どちらも `未充足`。`違反` は 0 行）／ 指標 0 行（段が未了）。****役割ごとの件数も手元と 1 つも違わない。****保護のかかった 2 枚にも確認を出さずに書けた。****準備日（`2025-11-01`）と片付け日（`2025-11-04`）には割り当てが 1 行も立たない**（**300 行は `2025-11-02` に 143 ／ `2025-11-03` に 157**。→ 上の「[生成シート 3 枚に書き戻した](#生成シート-3-枚に書き戻した)」）。**読み＋コア＋書き戻しで 4.790 秒。****外れた前提は無い** |
| 2026-09-22 | **Claude が Chrome で**（**承認は済んでいたので手動は無し**） | **同じスプレッドシート。****[`../src/`](.) の 3 ファイルを貼り直した**（[`count-violations.js`](count-violations.js) ／ [`name-unmet.js`](name-unmet.js) ／ [`generate.js`](generate.js)。**準備・片付けを需要に立てた** → [`ADR tech-requirements/0009`](../docs/ADR/tech-requirements/0009-準備と片付けを需要に立て規則3の5が当たらなくなった.md)）。**3 つとも SHA-256 の先頭 16 桁を突き合わせ、保存後にエディタを開き直して測り直した。****条件入力の「役割と必要人数」に `準備`・`片付け` の 6 行を足した**（**日を指定して時間帯は空欄**）。**`check-153.gs` も `check-210.gs` も 1 文字も直していない** | **項目 15**（**準備・片付けを需要に立てた後の M2**）＋ **書き戻し** | **手元と同じだった。違反 0 件・名指しされていない未充足 0 件で合格**（→ 上の「[準備・片付けを需要に立てた後に判定し直した](#準備片付けを需要に立てた後に判定し直した210-の続き)」）。**未充足 48 件・あと 212 人、突き合わせ 1038 ＝ 826 ＋ 212、超過 0 枠も手元と同じ。****案は 826 行とも同じで、SHA-256 の先頭 16 桁が一致した**（`08344ffd8a724630`）。**準備日（`2025-11-01`）が 0 → 342 行、片付け日（`2025-11-04`）が 0 → 88 行になった。****書き戻した 3 枚は 割り当て 826 行 ／ 検証結果 48 行 ／ 指標 0 行。****コアは 0.229 秒、読みは 2.217 秒、書き戻しまで含めて 6.399 秒。****外れた前提は無い** |

**1 回目の実行ログ**（抜粋。5 枚できて、保護が 3 枚）:

```
シート「条件入力」を作った
「条件入力」の 1 行目 1 列目から見出しを置いた
（…区画ごとに 10 行…）
シート「回答」を作った
シート「回答」に保護をかけた（警告のみ）
シート「割り当て」を作った
シート「検証結果」を作った
シート「検証結果」に保護をかけた（警告のみ）
シート「指標」を作った
シート「指標」に保護をかけた（警告のみ）
空のシート「シート1」を消した
5 枚のうち保護したのは 3 枚である
```

**2 回目**（項目 3）。**「作った」も「見出しを置いた」も 1 行も出ない:**

```
シート「回答」に保護をかけた（警告のみ）
シート「検証結果」に保護をかけた（警告のみ）
シート「指標」に保護をかけた（警告のみ）
5 枚のうち保護したのは 3 枚である
```

**見出しを 1 つ壊してから走らせた回**（項目 4）。**名指しで止まり、壊したセルは `日付XYZ` のまま残った:**

```
Error: 「条件入力」の 2 行目 1 列目が構成と違う。
いま: 日付XYZ / 準備開始 / 調理開始 / 調理終了 / 片付け開始 ／
構成: 日付 / 準備開始 / 調理開始 / 調理終了 / 片付け開始。
中身を見てから決める（黙って直さない）
```

**スタックトレースも日本語の関数名で出た**（`置き換える` → `見出しを置く` → `組み立てる` → `テンプレートを組み立てる`）。
| 2026-09-23 | **Claude が Chrome で**（承認は済んでいたので手動は無し） | **同じスプレッドシートに [`../src/`](.) の 8 ファイルを載せた**（**7 つ貼り直し ＋ [`assignment-grid.js`](assignment-grid.js) を新規**）。**エディタへは Windows のクリップボード経由で貼り、8 つとも SHA-256 の先頭 16 桁を突き合わせてから保存した**（**貼る前の 7 つが `main` と一致していることも同じ手で見た**）。**使い捨ての `check-213.gs` も足した。****条件入力は 1 行も触っていない** | **1 の再確認** ＋ **[#213](https://github.com/jun-eg/school-festival-shift/issues/213)**（マス目に書けるか ／ 読み戻して同じ行に戻るか） | **手元と 1 行ずつ同じ。****`buildTemplate` が 4 枚を作って 50 列に広げ、`8 枚のうち保護したのは 3 枚である` と出た。****割り当て 826 行・違反 0 件・未充足 48 件/212 人**で、**4 枚のマス目も 24×26/342 ／ 11×24/176 ／ 23×24/220 ／ 8×14/88 が手元と同じ。****書いたマス目をそのまま読み戻して 826 行に戻り、指紋も一致した**（`e8991d683ac4c02d`）。**実機でしか分からなかったのは 1 つ** — **時刻の見出しが `Date` で返る**（→ 上の「[割り当てを 4 枚のマス目にしてから走らせた](#割り当てを-4-枚のマス目にしてから走らせた213)」）。**外れた前提は無い** |
| 2026-09-23 | **Claude が Chrome で**（承認は済んでいたので手動は無し） | **同じスプレッドシート。****[`shell.js`](shell.js) だけを貼り直した**（**時刻の見出しを左に寄せる 1 行を足した** → [#213](https://github.com/jun-eg/school-festival-shift/issues/213)）。**SHA-256 の先頭 16 桁を突き合わせてから保存した。****条件入力も、ほかの `.gs` も 1 文字も動かしていない** | **2 周目**（マス目を入力として読み戻したうえでの 1 周 ／ 見出しの寄せ） | **1 周目と同じ案が出た。****走らせる前にマス目から戻った行が 826** で、**割り当て 826 行・違反 0 件・未充足 48 件/212 人・指紋 `e8991d683ac4c02d` も 1 周目と同じ。****マス目を挟んで 1 周まわしても、割り当てが増えも減りもしない。****見出しは左に寄り、`8:00` がその列の `準備` の真上に来た。****秒は 読み 1.405 ／ コア 0.179 ／ 書き 2.455 ／ 1 実行 4.039。****外れた前提は無い** |
| 2026-09-23 | **Claude が Chrome で**（承認は済んでいたので手動は無し） | **前の回と同じスプレッドシート。[`../src/`](.) の 9 ファイルを貼り直し**（`sheet-layout` ／ `input-types` ／ `core` ／ `count-violations` ／ `name-unmet` ／ `shell` ／ `verify-structure` ／ `generate` ／ `assignment-grid`）。**`buildTemplate` で「置き方のルール」の区画を足し、使い捨ての `check-215.gs` と `put-215.gs` を 2 つ足した** | **15**（**まとまりと散らしを入れてからの M2** → [#215](https://github.com/jun-eg/school-festival-shift/issues/215)） | **案が手元と同じだった。**割り当て 826 行 ／ 違反 0 件 ／ 未充足 48 件・あと 212 人 ／ **指紋 `12384a47f42c4d80` が 3 回とも一致。****「連続して入る最小の長さ」のセルは `Date` で返る**（`1:00` → `01:00` に揃って 60 分）。**`1:20` と「項目が空」は、どちらも名指しして止まった。****続けて生成シートに書き戻した** — **マス目 4 枚が 24×26/342 ／ 11×24/176 ／ 23×24/220 ／ 8×14/88 で、埋まったセルの合計 826 ＝ 割り当ての行。読み戻しても 826 行・同じ指紋に戻る。1 実行 8.227 秒**（→ 上の「まとまりと散らしを入れてから判定し直した」） |
| 2026-09-23 | **Claude が Chrome で**（承認は済んでいたので手動は無し） | **前の回と同じスプレッドシート。****[`../src/`](.) の 10 ファイルを載せた**（**7 つ貼り直し**: `sheet-layout` ／ `input-types` ／ `core` ／ `shell` ／ `menu` ／ `generate` ／ `assignment-grid`。**3 つ新規**: `fairness-metrics` ／ `distribution-image` ／ `export-images.html`）。**貼る前に、実機の 7 つが #215 の回の版と、残り 9 つが `main` と一致することを、貼った後に `.js` 17 ＋ HTML 2 が `main` と一致することを、どちらも SHA-256 の先頭 16 桁で見た**（**エディタを開き直してから測り直している**）。**使い捨ての `check-223.gs` と `check-223-cells.gs` を足した。****条件入力も回答も 1 セルも触っていない** — **書き換えたのはマス目の `学祭1日目` の 2 行目と 12 行目だけである** | **19 ・ 16 ・ 17 ・ 18**（[#223](https://github.com/jun-eg/school-festival-shift/issues/223)。**#154〜#157 の実機の残り**） | **19 は手元と同じ** — **段を差し替えずに「生成」を 1 回押して、割り当て 826 行 ／ 検証結果 48 行 ／ 指標 36 行が書かれ、3 つとも指紋が一致した**（`12384a47f42c4d80` ／ `a98c4caf693eaa59` ／ `96ca9aa72973e3a8`）。**氏名 36 行・和 413 ／ 177 ／ 66・強調 0。1 実行 10.117 秒。****16・17 は手元と同じ** — **1 セルずつなら `onEdit` が走り（7.2〜9.9 秒）、赤い太字・メモの印・一言が出た。生成し直すと、印のセルが残り、置けない手直し 2 件が `食い違った固定` で先頭とメモに出て、3 つとも手元の `build` と指紋が一致した**（`e5446757a519c0e3` ／ `02546b2ddbb379af` ／ `204aabf1d5ba67a0`。**1 実行 10.017 秒**）。**外れた前提が 1 つ**: **続けて 2 セル書き換えると `onEdit` が 1 回しか走らず、2 手目のセルに印が付かない**（**2 回踏んで 2 回とも**。**その手直しは次の「生成」で組み直される**）。**18 は確かめた。ただし保存はリンクではなく右クリックである** — **ダイアログが開き、4 枚とも描けて手直しの後の案が載っていた。「保存する」も「4 枚とも保存する」も PNG を落とさなかった**（**最上位のページからの対照のダウンロードは落ちた**）。**右クリックで保存でき、LINE ノートに貼ってスマホで拡大すると役割名が読めた**（**人の手で見た**）（→ 上の「[指標から画像まで 1 周通した](#指標から画像まで-1-周通した223)」） |

## 確認に使うファイルの規約

| | |
| --- | --- |
| **実データを 1 行も入れない** | **氏名・学籍番号を 1 行も入れない**（→ 2「個人情報が通る経路」）。**入れるなら [`data/`](../data/) のモックである**（7「判定に使う入力データ」と同じ規約）。**2026-09-20 の回で入れた値は `x` と `日付XYZ` の 2 つだけである。**
**[#185](https://github.com/jun-eg/school-festival-shift/issues/185) の回で入れたのは条件入力の 5 区画だけで、
日付・時刻・役割名・人数・学年しか無い**（**回答シートは 1 行も埋めていない** → 氏名も学籍番号も 1 つも入っていない）。
**[#199](https://github.com/jun-eg/school-festival-shift/issues/199) の回は回答シートを埋めたが、入れたのは
[`data/前回の希望データ-モック.csv`](../data/前回の希望データ-モック.csv) の 50 行だけである**（7「判定に使う入力データ」と同じ規約）。
**入る側から送った 1 件も、そのモックの 1 行の値をそのまま入れた。****名簿の画像も、役割と人数しか書いていない作りもののである**。**[#223](https://github.com/jun-eg/school-festival-shift/issues/223) の回に書き換えたのはマス目のセルだけで**（**役割名を入れる／空にする**）、**氏名・学籍番号は 1 つも足していない。****LINE ノートに貼ったのは、モックから描いた画像だけである**（**氏名の列はモックの名前である** → [#222](https://github.com/jun-eg/school-festival-shift/pull/222)） |
| **画像の置き場** | **[`docs/画面の記録/`](../docs/画面の記録/)`<連番>-<何の画面か>.png`。連番は `0001` から振り、欠番を詰めない・振り直さない。** **入れる前に、氏名・学籍番号が 1 枚も写っていないことを確かめる**（→ 2「個人情報が通る経路」）。**メールアドレスが写るなら、不透明に塗ってから入れる**（透ける塗り方をしない）。**アドレスバーに使い捨てのトークンが写るときも同じく塗る**（承認の画面の URL には `part=` が付く。使い終わって効かないが、リポジトリに残す理由が無い）。初回の権限承認は**撮って置いた**（上の「初回承認で出た画面」）。**貼るのは [#159](https://github.com/jun-eg/school-festival-shift/issues/159) の手順書である。** **フォルダは最初の 1 枚と同じ変更で作る**（空のフォルダを先に置かない → [`docs/ADR/README.md`](../docs/ADR/README.md)） |
| **確認に使ったスプレッドシートは残さない** | **テンプレートの原本と混ざる。**見終わったら消す（**残すのは、この文書の記録と画像だけである**） |
